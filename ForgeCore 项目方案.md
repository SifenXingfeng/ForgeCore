# AI 驱动的智能工厂数字孪生设计与仿真平台项目方案

> **项目名称：ForgeCore**
>
> **中文名称：ForgeCore AI 智能工厂数字孪生平台**
>
> **项目定位：面向生产制造场景的可视化工厂设计、生产仿真、智能物流与 AI 优化平台**
>
> **核心理念：Design → Build → Simulate → Analyze → Optimize**

---

# 1. 项目概述

## 1.1 项目背景

随着数字孪生、智能制造、工业互联网、人工智能和三维可视化技术的发展，传统工厂正在逐步从单纯的信息化管理转向数字化、智能化与自动化。

现有大量工业管理系统通常侧重于设备监控、生产数据展示、订单管理或故障告警，而对于生产线本身的设计、工艺流程模拟、物流调度以及整体产能优化支持较为有限。

本项目计划开发一套以 **3D 数字工厂为核心** 的智能工厂设计与仿真系统。

用户可以在一个基于网格的三维空间中自由搭建工厂，通过放置机器、货物仓库、开放式货架、无人运输车辆、无人机等设施，并拖拽铺设支持直线与自动弯道的可运行传送带，自定义完整的生产流程，再通过系统提供的生产仿真引擎观察工厂运行情况。

平台不绑定某一种固定工业场景，而是允许用户自行定义：

- 原材料
- 半成品
- 成品
- 加工机器
- 加工关系
- 生产流程
- 仓储方式
- 运输方式

从而满足不同制造行业和实验场景的需求。

系统同时内置 AI 智能分析模块，可以根据工厂运行过程中产生的数据自动分析产线瓶颈、物流拥堵、设备利用率、仓储效率以及整体产能，并向用户提出针对性的优化建议。

长期目标是使系统进一步具备自动优化甚至自动设计生产线的能力。

---

# 2. 项目定位

ForgeCore 并不是一个单纯用于展示工厂状态的传统数字孪生系统。

本项目希望构建的是：

> **一个允许用户自由设计、搭建、运行、分析并优化数字工厂的通用智能制造仿真平台。**

平台主要覆盖五个阶段：

```text
设计 Design
    ↓
搭建 Build
    ↓
仿真 Simulate
    ↓
分析 Analyze
    ↓
优化 Optimize
```

用户可以从一块空白工厂区域开始，根据自己的实际需求建立完整生产系统。

---

# 3. 项目核心目标

本项目需要实现以下核心目标：

1. 构建可自由编辑的 3D 数字工厂
2. 支持基于网格的机器与设施布局
3. 支持多楼层工厂建设
4. 支持用户自定义任意物品
5. 支持用户定义加工配方和生产工艺
6. 支持机器之间通过传送带建立完整生产流程
7. 支持物品在生产线上真实移动
8. 支持仓储及库存管理
9. 支持 AGV 无人运输车辆自动物流
10. 支持无人机跨区域、跨楼层运输
11. 支持复杂生产网络和物流网络
12. 支持实时生产数据统计
13. 支持生产瓶颈检测
14. 支持 AI 分析生产线运行状态
15. 支持 AI 提出产能优化方案
16. 支持优化方案仿真验证
17. 为未来 AI 自动工厂布局和智能生产规划预留扩展能力

---

# 4. 系统总体设计理念

整个系统建立在几个基础抽象之上：

```text
Item
物品

Recipe
加工配方

Machine
机器

Storage
仓储

Transport
运输

Factory Node
工厂节点
```

所有工业场景均通过这些通用对象组成。

系统不会将某种机器和具体工业用途绑定。

例如系统不会规定：

```text
Machine-A 必须加工钢材
```

而是允许用户定义：

```text
Machine-A

输入：
Item-001

输出：
Item-002
```

因此同一套系统既可以表示：

```text
铁矿石
↓
铁锭
```

也可以表示：

```text
牛奶
↓
奶酪
```

还可以表示：

```text
PCB
+
芯片
+
电子元件

↓

主板
```

这种抽象设计可以使 ForgeCore 适用于：

- 机械制造
- 汽车工业
- 食品加工
- 电子制造
- 化工生产
- 物流仓储
- 自动化生产实验
- 教学演示

等多种场景。

---

# 5. 系统总体架构

```text
┌─────────────────────────────────────────────┐
│                   用户                       │
└────────────────────┬────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│               Web 交互界面                   │
│                                             │
│ 3D工厂 │ 工厂编辑器 │ 数据面板 │ Factory Agent│
└────────────────────┬────────────────────────┘
                     │
              HTTPS / SSE
                     │
                     ▼
┌─────────────────────────────────────────────┐
│                后端业务系统                  │
│                                             │
│ 工厂服务 │ 物品服务 │ 配方服务 │ 仿真服务    │
│ 仓储服务 │ 物流服务 │ 数据服务 │ Agent服务   │
└──────────────┬────────────────┬─────────────┘
               │                │
               ▼                ▼
      ┌─────────────────┐  ┌────────────────┐
      │      数据库      │  │    AI系统      │
      │                 │  │                │
      │ PostgreSQL      │  │ LLM            │
      │ Redis           │  │ Agent          │
      └─────────────────┘  │ 优化算法       │
                           └────────────────┘
               │
               ▼
      ┌─────────────────────────┐
      │       工厂仿真引擎       │
      │                         │
      │ 物品流动                │
      │ 机器加工                │
      │ 物流运输                │
      │ 仓储库存                │
      │ 产能计算                │
      │ 时间模拟                │
      └─────────────────────────┘
```

---

# 6. 3D 数字工厂编辑器

3D 数字工厂是整个项目的核心。

用户进入系统后，可以新建一个数字工厂。

创建工厂时可以设置：

```text
工厂名称

工厂长度

工厂宽度

初始楼层数量

网格尺寸
```

例如：

```text
Factory A

100 × 100

Grid Size：
1m × 1m

Floor：
3
```

整个工厂由规则网格组成。

用户可以在网格上放置不同工业设施。

---

# 7. 网格建造系统

所有主要工业设备放置在统一网格中。

例如：

```text
□ □ □ □ □ □ □
□ M M □ □ R R
□ M M → → R R
□ □ □ □ □ □ □
□ A □ □ □ □ □
```

其中：

```text
M = Machine

R = Rack

A = AGV

→ = Conveyor
```

网格系统主要负责：

- 设施定位
- 设施碰撞检测
- 道路规划
- 设备占地判断
- AGV 寻路
- 多楼层坐标统一

每个对象拥有：

```text
X

Y

Floor

Width

Length

Rotation
```

---

# 8. 多楼层工厂系统

ForgeCore 支持从地面层向上堆叠的多楼层数字工厂。首版楼层数据必须显式记录 `id`、`level`、`name`、`elevationM` 与 `heightM`；新建上层时以当前最高层顶部作为新层标高，层高允许在 `2.5–12m` 范围调整，默认 `4.5m`，从而保证任意连续楼层都具有确定的三维高度关系。

工厂编辑器左侧提供楼层选择器：

```text
Floor

5F
4F
3F
2F
1F
```

用户可新建上层并随时切换当前楼层。每个楼层条目右侧提供独立显示开关，创建或恢复楼层时默认开启；关闭后，该层对象在任何显示模式下都不得出现，但切换到该层时仍只绘制该层网格供建造。楼层面板提供三档材质范围设置：`仅当前层` 只显示当前楼层中已开启的对象；`多层透视` 显示所有已开启楼层，并把非当前层对象统一作为半透明上下文；`多层实显` 以正常材质显示所有已开启楼层。多层模式不再隐含“只显示当前层以下”的方向限制，当前层上方和下方的已开启楼层一视同仁，均按真实相对标高显示且保持只读。跨层倾斜传送带只有其两个端点所属楼层都处于开启状态时才显示，避免关闭楼层的坡面仍泄露到场景。

三档模式始终只绘制当前楼层网格，不叠加任何其他楼层网格。只要多层模式中存在至少一个已开启的非当前楼层，当前层实体地块就改为单面的半透明轻量表面，网格材质与承载面均不得写入深度缓冲；底部承托面移动到最低可见楼层下方，镜头目标与距离按最低、最高可见标高形成的完整垂直范围自动调整。可见的跨层传送带及其在途物品使用高于半透明地块与网格的透明绘制优先级，同时继续接受不透明设施的深度遮挡，保证坡面穿过网格面时连续可见而不会透过机器。切回 `仅当前层` 或关闭全部其他楼层时恢复单层构图。楼层开关只控制可见性，不删除对象、不改变碰撞、库存、任务或仿真数据；当前实现仍不提供删除楼层，避免在跨层连接和库存迁移规则尚未定义前破坏数据关系。

用户因此可以建设：

```text
1F 原材料与重型加工

2F 半成品加工

3F 装配

4F 检测

5F 成品仓储
```

等复杂工厂。

---

# 9. 自定义物品系统

ForgeCore 的生产系统建立在统一的 `Item` 物品对象之上。

用户可以创建任意物品。

`Item` 是业务身份，`ItemModel` 是可复用的视觉形态：多个原材料、半成品或产品可以引用同一个稳定 `modelId`，再用各自的 `modelParameters` 改变尺寸、截面、孔径、齿数、圈数、颗粒数、颜色和 PBR 材质等视觉参数，不需要为“钢板”、“铜板”和“塑料板”分别复制网格。

基本属性：

```text
Item ID

物品名称

物品编号

物品类别

描述

3D模型

图标

尺寸

重量

最大堆叠数量
```

字段边界必须保持清晰：

- `Item` 保存 `Item ID`、名称、编号、类别、描述、图标、`modelId`、`modelParameters`、质量与最大堆叠数等业务数据。
- `ItemModel` 保存稳定 `modelId`、参数 schema、默认参数、材质槽、GLB 路径、预览路径、包围盒、几何指标、版本与 SHA-256。
- `Item.modelParameters` 只保存用户明确调整过的参数覆盖，不复制整份默认参数；读取时由 `modelId` 对应 schema 补齐其余默认值。恢复旧存档时应删除未知键、业务字段，并把旧版完整且未调整的默认参数表折叠为空覆盖。显式覆盖可以等于 schema 默认数值，例如用户仍可把铜材质的金属度明确设为 `0.5`。
- `Mass` / 重量和 `Stack Size` / 最大堆叠数是 `Item` 的物流与业务属性，**不是模型参数**，不得写入几何生成器或用它们改变网格。

“物品与模型库”页面同时承担业务物品管理入口。已添加物品必须以独立清单显示名称、编码、类别、基础模型、显式参数覆盖数、质量和最大堆叠数，并提供编辑与删除操作。编辑沿用创建时的参数化建模器，允许修改名称、编码、类别、描述、基础模型、模型参数、质量和最大堆叠数；保存时保持原 `Item ID` 不变，使配方、库存、传送带与 AGV 引用继续指向同一业务物品。切换基础模型时清空旧模型参数并载入新 schema 默认值，保存后场景中所有该物品实例按新模型与参数实时重建。

物品名称和编码不能为空，同一工厂内编码不区分大小写且必须唯一；质量必须为有限非负数，最大堆叠数必须为正整数，模型必须来自当前审计目录。删除仍执行引用门禁：只要物品被配方、AGV 程序、AGV 车载货物、非零/无限/已预约库存引用，就必须拒绝并说明原因；未被引用的零库存记录随物品一起清理。

例如：

```text
Item ID：
MAT-001

名称：
铝合金板

类型：
Raw Material

重量：
2.6 kg

最大堆叠：
20
```

---

# 10. 自定义物品模型

系统已提供首版 **36 个 ForgeCore 原创参数化默认物品模型**。它们是项目自有的 **first-party/core** 资产，权威运行时路径为 `assets/3d/core/items/v1/`，不属于 `assets/3d/vendor/` 的上游原包，也不属于 `assets/3d/derived/` 的第三方派生资产。

首版使用五类稳定 `modelId`：

- **基础形状（basic，6）**：`BASIC_BOX`、`BASIC_CYLINDER`、`BASIC_SPHERE`、`BASIC_DISC`、`BASIC_CONE`、`BASIC_RING`。
- **原材料（material，10）**：`RAW_CHUNK`、`RAW_GRANULE`、`RAW_INGOT`、`RAW_LOG`、`MATERIAL_PLATE`、`MATERIAL_ROD`、`MATERIAL_BEAM`、`MATERIAL_PIPE`、`MATERIAL_COIL`、`MATERIAL_WIRE_COIL`。
- **机械零件（mechanical，8）**：`PART_GEAR`、`PART_SHAFT`、`PART_BEARING`、`PART_BOLT`、`PART_NUT`、`PART_SPRING`、`PART_WHEEL`、`PART_FLANGE`。
- **电子与机电部件（electronic，5）**：`ELEC_PCB`、`ELEC_CHIP`、`ELEC_BATTERY`、`ELEC_MOTOR`、`ELEC_MODULE`。
- **包装与容器（package，7）**：`PACK_BOX`、`PACK_CRATE`、`PACK_BIN`、`PACK_PALLET`、`PACK_SACK`、`CONTAINER_DRUM`、`CONTAINER_BOTTLE`。

`assets/3d/core/items/v1/catalog.json` 是运行时模型索引，包含参数 schema、默认值、材质模板、包围盒、三角形数、预览路径和文件哈希；`assets/3d/core/items/v1/SHA256SUMS` 覆盖全部 36 个 GLB、36 张 PNG 预览和 `catalog.json`。结构参数改变时应重新生成几何，非结构材质参数可在运行时复用几何。同款 Item 批量出现时应使用实例化渲染。

参数面板必须读取 schema 的类型、范围、步长、枚举和 `activeWhen` 条件依赖，而不是在前端另写一套参数规则。例如 `wallThickness` 仅在圆柱体 `hollow=true` 时生效，电池 `diameter` 仅在 `shape=cylindrical` 时生效。颜色、金属度、粗糙度、透明度和自发光可覆盖主材质；`texture` 是应用层管理的纹理绑定键，离线生成器不读取网络地址或隐式引入外部资源。

创建物品对话框已直接读取上述 schema，为 `number`、`integer`、`boolean`、`enum`、`color` 和 `string` 六类参数生成对应控件，显示范围、步长、单位和条件依赖，并提供实时三维预览与一键恢复默认值。结构、拓扑和包围盒参数通过与离线资产完全相同的 ForgeCore 原创几何定义在浏览器中重新生成网格；材质预设、颜色、金属度、粗糙度、透明度、自发光和程序纹理键实际作用于主材质。用户触碰的参数会被限幅、规范化后作为显式覆盖保存，未知参数以及 `massKg`、`maxStackSize` 等业务字段无法进入模型生成链路。

内部坐标与格式契约固定为：**米制、右手系、Y-up、落地中心轴点（ground-center）、自包含 GLB 2.0**。未修改参数的默认形态继续直接加载已审计 GLB；带参数覆盖的 first-party/core Item 允许在浏览器中复用同一原创几何定义生成 Three.js `BufferGeometry`，这不是新增外部资产格式，也不得绕过 catalog schema。第三方和用户导入的静态资产运行时仍只加载 GLB，不依赖外部 URI，不在客户端直接解析 FBX、OBJ 或松散 glTF；其他输入格式必须先转换成自包含 GLB 才能进入运行时目录。

模型库由项目自带的 Node.js 脚本确定性生成，不需要 `npm install` 或任何第三方包：

```powershell
node tools/item-models/build.mjs
node tools/item-models/validate.mjs
```

首版需求导入源 `D:/Downloads/ForgeCore 默认基础 3D 物品模型库设计清单.md` 的 SHA-256 为 `8848a43224a2c4fe8663159a4272694f4353097d76560a7558eceab35ada9b16`；该外部文件只是初始导入需求，后续产品范围和技术契约仍以本方案为唯一权威。36 个资产不含第三方网格或纹理，但 `assets/3d/core/items/v1/` 根目录当前没有独立许可文件；在项目所有者正式设定和记录许可之前，不得把该库对外宣称为 CC0、CC BY 或其他开放许可资产。

详细指标、逐文件 SHA-256、验证项和原创/许可边界见 `assets/3d/ASSET_AUDIT.md`。

同时支持用户上传自己的模型。

运行时格式：

```text
GLB
```

例如用户可以上传：

```text
engine.glb
```

并建立：

```text
Item：

发动机
```

之后这件发动机模型就可以真正出现在数字工厂中。

---

# 11. 生产配方系统

机器本身不直接绑定原材料和成品。

系统通过 `Recipe` 定义加工关系。

例如：

```text
Recipe ID：
REC-001

名称：
铁板加工

输入：

钢材 × 2

输出：

钢板 × 1

加工时间：

5秒
```

当机器使用该配方时：

```text
钢材 ×2

↓

机器加工5秒

↓

钢板 ×1
```

初版配方工作区必须提供完整的创建与维护闭环：新建、编辑、启用/停用和删除配方；每条配方包含 `1–3` 种不重复的输入物品和 `1–3` 种不重复的输出物品，并分别设置引用物品与数量，同时维护配方编码、名称、工艺说明和处理时间。界面与状态层必须共同执行三种上限、引用存在性、数量和处理时间校验，不能只依赖表单按钮。配方不能引用不存在的 Item；空物品库进入配方页面时应明确说明这一前置条件，并提供直达物品创建页面的操作入口，不得只显示无法点击或会被其他浮层遮挡的按钮。

---

# 12. 多原料加工

配方支持多个输入，首版上限为三种不重复原料。

例如：

```text
Recipe：

电机装配
```

输入：

```text
线圈 × 2

磁体 × 4

外壳 × 1

```

输出：

```text
电机 × 1
```

因此系统能够模拟复杂装配生产。

---

# 13. 多产品加工

配方还可以支持多个输出，首版上限为三种不重复产物。

例如：

```text
原材料 A ×10

↓

Processing

↓

产品 B ×4

产品 C ×3

副产品 D ×2
```

后期还可以加入：

```text
概率产品

废品率

产品质量

随机损耗
```

等机制。

---

# 14. 通用机器系统

建造栏只提供一个默认通用机器，不预设加工机、装配机、熔炼设备等业务分类。机器外观只是视觉底座，不代表真实工艺；名称、配方和生产语义均由用户自行定义。

通用机器视觉底座固定采用机器候选图中的第五个模型，即已审计的 Kenney `machine-fortified.glb`（候选 E“强化加工设备”）；原 `machine.glb` 仅作为 vendor 原包内容保留，不再作为正式通用机器运行模型。新建机器、建造预览和旧存档恢复必须统一指向候选 E。通用机器由原 4×4 扩大一圈为 `6m × 6m` 逻辑占地，候选 E 外壳按约 X/Z 5.4m、高 4.8m 适配，四周各保留约 0.3m 的占地余量；机内不再只有一条居中输送线，而是在本地 Z=`-1/0/+1m` 设置三条约 4.32m 长、0.88m 宽的贯穿传送带，分别对应编号 1–3 的输入/输出端口。两端黑色软帘位于本地 X=`±2.37m`，每侧 11 条、总宽约 3.45m、高约 2.34m，覆盖三条通道并完整落在放大的外壳开口内。机器实际进入 `processing` 状态时，三条内部传送带的流向标记随外部仿真时间运行，机器中央红色工作灯与内部红色光源同步高亮；等待原料、阻塞或暂停时不得继续伪装成正在加工。机器上方使用随机器共同旋转的黄色方向箭头，六个端口分别标为“入料口 1–3”“出货口 1–3”；工作灯、箭头、端口标签和选中标签必须随外壳同步升高，不得陷入机身或彼此遮挡。

系统核心使用统一的：

```text
Machine
```

对象。

Machine 基础属性：

```text
Machine ID

名称

机器模型

尺寸

输入接口数量

输出接口数量

加工速度

当前配方

缓存容量

运行状态

设备功率

运行时间
```

---

# 15. 机器输入与输出接口

机器拥有明确输入口和输出口。

例如：

```text
       OUTPUT
          ↓

    ┌──────────┐
→ → │ Machine  │ → →
    │          │
→ → │          │
    └──────────┘
```

首版通用机器固定提供 `3 Input / 3 Output`，这也是首版单机端口上限。未旋转时三个入料口位于机器本地 `-X` 侧，三个出货口位于本地 `+X` 侧，并按本地 Z=`-1/0/+1m` 编号为 1、2、3。传送带只能从出货口起始、在入料口结束；每个编号端口最多连接一条传送带，成功连接后立即退出吸附候选，删除该传送带后才重新变为空闲。端口占用检查必须在指针吸附和状态写入两层执行，防止并发或旧界面状态造成重复连接。

机器的 `rotationY` 改变时，模型、三条内部传送带、黑色软帘、方向箭头和六个逻辑接口作为一个整体旋转。建筑物尚在鼠标上时，滚轮向上左旋 90°、向下右旋 90°，每次有效滚轮输入只改变一个四分之一圈；落地后仍可按 `Q` 左旋 90°、按 `E` 右旋 90°，属性浮窗同时提供等价按钮。快捷键必须在编辑器文档级捕获阶段监听，同时兼容 `KeyboardEvent.code=KeyQ/KeyE` 与字母键值，避免画布或按钮焦点导致失效；输入框、文本域、下拉框、可编辑区域、输入法组合态和带 Ctrl/Meta/Alt 的组合键不得触发机器旋转。旋转后所有已登记上下游关系的传送带必须根据各自端口编号重新对齐，不得串口或仍连接旧方向。

机器属性浮窗必须按编号展示六个端口的空闲/已连接状态。绑定单输出配方时，所有已连接出货口自动输出唯一产物，不显示多余选择；绑定多输出配方时，每条已连接的出货传送带分别选择该配方允许的一种产物，未分配的出货口不派发物料。修改配方后，已失效的出货分配必须清空，不能继续输送不在新配方中的产物。

这样可以使生产线设计更加真实。

---

# 16. 传送带系统

传送带是整个生产系统的主要运输设施。

用户先在底部建造栏单击传送带，进入持续建造态；随后在网格上按住并沿期望线路拖动，释放后创建传送带。开始铺设时，指针接近机器任一空闲出货口即自动吸附并登记 `fromObjectId + fromPortIndex`；释放前接近另一台机器任一空闲入料口即自动吸附并登记 `toObjectId + toPortIndex`，界面明确提示端口编号与“连接成功”。已占用端口不得再次吸附。吸附使用机器旋转后的真实端口坐标，不能再用任意边界中点代替输入/输出语义。对边界 `{x:10,z:10,width:6,depth:6}`、0° 的机器，入料口 1–3 为 `(11,12)/(11,13)/(11,14)`，出货口 1–3 为 `(15,12)/(15,13)/(15,14)`；旋转 90° 后，入料口为 `(12,15)/(13,15)/(14,15)`，出货口为 `(12,11)/(13,11)/(14,11)`。端口锚点距中心 2m，处在距中心 2.37m 的黑帘之后，并与半长 2.16m 的内部带形成连续重叠，视觉上不得在机器和传送带之间留下断口。未靠近端口时仍允许创建自由传送带。

所有普通与跨层传送带的首尾端点也属于可吸附连接节点，但场景不额外绘制端点小球；可吸附状态只通过建造预览、状态文字和线路高亮反馈，保持工厂画面干净。用户从空闲输出端开始或在空闲输入端结束拖线时，系统自动建立传送带之间的 `fromObjectId` / `toObjectId` 邻接关系，并继续遵守单端点单连接和流向语义。当相邻两段在共享端点的水平投影方向不同，连接点必须改用方向匹配的 Kenney 弯道组件，并同时裁切前后两段直线或坡面，禁止用两个直线端头交叉、重叠或留下小球代替转角；普通传送带与跨层倾斜传送带的混合连接同样适用。物品到达一段传送带末端后必须原样转交给下一段 TransitItem，允许普通传送带、倾斜传送带和普通传送带组成连续链路；删除中间段只解除相邻关系，不得连带删除整条生产线。

系统记录用户实际经过的网格轨迹，而不是只根据起点和终点强制生成固定 L 形路线：直线拖动只产生直线段，只有拖动轨迹真实改变水平/垂直方向时才形成拐点并使用弯道组件。弯道组件必须独占拐点格，Kenney 弯道原件未旋转时的有效接口方向按 `-X/+Z` 处理；四向映射固定为 `-X/+Z → 0°`、`+X/+Z → +90°`、`+X/-Z → 180°`、`-X/-Z → -90°`，旋转后的两个开放接口须分别朝向前后路径。相邻直线段必须截短至接口边缘，不得穿入弯道网格或与弯道模型叠放。两个相邻拐点之间若没有足够空间容纳完整直线模块，则由弯道接口直接衔接，不得强行缩放直线模型形成畸形碎片。再次单击已选中的传送带建造项、按 `Esc` 或在建造态中单击鼠标右键，均须立即退出建造态并清除鼠标随行预览。

放置门禁按网格占用而不是只看视觉模型：任意两个建筑占地矩形不得重叠；任意两条传送带的 1m 通道不得共格、交叉或叠放；新建筑也不得覆盖已有传送带，传送带不得穿过非首尾连接建筑。建筑与传送带恰好在边界相接允许，三条相距 1m 的并行端口通道必须判为互不冲突。预览层显示不可放置只用于即时反馈，最终写入 Store 前必须再次执行同一碰撞校验。

例如：

```text
Machine A
   ↓
────────────
   ↓
Machine B
```

传送带拥有：

```text
Transport Speed

Direction

Capacity

Connected Node
```

---

# 17. 真实物品运输

物品不是简单修改数据库状态，而需要在 3D 工厂中真实移动。

例如：

```text
■ → ■ → ■ → ■ → Machine → □ → □
```

物品实体拥有：

```text
Item ID

当前位置

运输设施

移动方向

移动速度

目标节点
```

用户可以直接观察：

```text
原材料

↓

进入机器

↓

机器加工

↓

出现新产品

↓

继续沿传送带运输
```

这一过程。

---

# 18. 分流器系统

复杂生产线需要支持物品分流。

例如：

```text
             → Machine A
──────────── Splitter
             → Machine B
```

分流规则可以包括：

```text
平均分配

左侧优先

右侧优先

按比例

按照物品类型
```

例如：

```text
Item A
→ Left

Item B
→ Right
```

---

# 19. 合流器系统

多个生产线路可以合并。

```text
Line A ───┐
          ├──── Main Line
Line B ───┘
```

系统需要处理：

```text
线路优先级

等待队列

传送带拥堵

合流顺序
```

---

# 20. 缓冲区系统

不同机器的生产速度通常不同。

例如：

```text
Machine A

生产速度：
60/min

Machine B

生产速度：
30/min
```

如果直接连接：

```text
A
↓
B
```

可能导致传送带堵塞。

因此系统提供：

```text
Buffer
```

例如：

```text
Machine A
   ↓
 Buffer
   ↓
Machine B
```

Buffer 可以存放一定数量的中间产品。

例如：

```text
容量：

500
```

这一系统能够明显增强生产仿真的真实性。

---

# 21. 跨层运输系统

不同楼层之间需要专门运输设施。

系统提供：

## 跨层倾斜传送带

适合：

```text
连续生产
```

首版采用真实倾斜的连续输送段连接相邻两层，而不是瞬移式竖井。放置前可选择“向上运输”或“向下运输”；方向决定路径起终点、上下层端点的输入/输出语义和物品的三维运动方向。倾斜段记录 `fromFloorId`、`toFloorId` 与 `riseM`，并以默认 `4.5m` 高差、`6m` 水平投影形成的 `75%` 坡度（约 `36.87°`）作为全局固定坡度；当相邻层高为 `h` 时，水平投影必须同步变为 `h / 0.75`，不得只拉伸 Y 轴导致坡面变陡。低层端点保持网格吸附，高层端点按精确水平投影生成，模型长度、预览、两层碰撞占地、续接端点、三维运输长度和在途位置必须共用同一动态路径。滚轮四向旋转只改变坡面的水平前进方向：模型本地 X 轴沿三维坡向，本地 Z 宽轴始终保持在世界水平面内，再由二者求出坡面法线；不得使用会在南北方向产生侧滚的任意最短旋转。

倾斜传送带可独立吸附机器或货物仓库端口，也可吸附普通/跨层传送带的空闲端头。货架不提供输送端口，不参与普通或跨层传送带吸附。放置完成后，上一层露出的自由端点能够直接继续拖绘普通传送带；同理，下行模式允许从高层接入并向低层输出。属性面板提供方向反转操作，反转时同步交换路径、楼层端点和上下游关系，不能只翻转视觉箭头。

---

## 货运升降机

适合：

```text
托盘

大型货物

批量运输
```

特点：

```text
运输容量高

运输周期较长
```

---

## 无人机

适合：

```text
小批量

紧急

高价值

跨楼层运输
```

---

# 22. 仓储系统

系统支持不同规模仓储设施。底部建造栏同时提供可运行的“货物仓库”和“货架”，两者是模型、占地和容量规则均不同的独立对象。货物仓库继续使用 `rack` 对象类型以兼容已有存档，开放式货架使用新的 `shelf` 对象类型，禁止再以同一种设施或同一容量规则混用。

包括：

```text
Buffer

Rack

Warehouse
```

用户可以自由放置货物仓库。首版仓库固定采用候选图第六个 Kenney `machine-window.glb`（候选 F“可视化加工舱”）作为 CC0 外壳，统一适配为 `6m × 6m` 占地和约 `5.4m × 5.4m`、高 4.8m 的可见尺寸。仓内三条输送通道位于本地 Z=`-1/0/+1m`，内部摆放已入库的 Kenney `box-small.glb` / `box-wide.glb` 纸箱作为货物容器视觉，两端在 X=`±2.37m` 设置覆盖三条通道的黑色软帘。纸箱模型只负责“仓内装有货物”的视觉表达，准确数量、品类和容量必须读取 Inventory，不得根据画面中的箱子个数反推业务库存。

货物仓库属性：

```text
Warehouse ID

仓库尺寸

格口数量

格口容量

存储类型

当前库存

已定义物品明细

有限库存 / 无限供应状态

出货口 1–3 独立出货间隔（秒 / 次）
```

选中仓库后，属性浮窗必须直接列出项目中已经定义的全部 Item，显示物品名称、编码、该仓库实际数量和供应方式，不得只显示总数或前几项摘要。没有任何 Item 时显示“先定义物品”的明确前置引导；至少存在一种 Item 后，用户可以在对应行输入数量并手动入库，也可减少误加库存。仓库的有限库存总量继续受聚合容量约束。

首版仓库拥有编号 1–3 的三个入货口和三个出货口。每个端口只能连接一条传送带；连接成功后不再参与吸附，删除传送带后恢复空闲。端口坐标、旋转、吸附、帘后锚点和碰撞规则与 6×6 通用机器共用同一协议，区别只在仓库输入端显示“入货口”而非“入料口”。仓库在手上时同样支持滚轮 90° 步进旋转。历史存档中的 `kind: rack` 始终解释为 6×6 候选 F 货物仓库并为旧连接分配不重复端口；新货架只使用 `kind: shelf`，不得通过重解释旧 `rack` 存档完成迁移。

每个仓库的三个出货口分别配置 `dispatchIntervalSecByPort[0..2]`，表示对应端口每隔多少仿真秒最多发出 1 件货物。每个端口允许 `0.25–60` 秒，默认均为 `2.5` 秒；属性浮窗不再提供覆盖整个仓库的大面板统一间隔，而是在“出货口 1–3”的端口行内分别编辑。三个端口使用独立冷却计时器，只有实际成功创建 TransitItem 的端口重置自己的冷却；无货、未选物品或线路阻塞时该端口保持可立即重试，任一端口出货不得推迟另外两个端口。间隔只控制仓库创建 TransitItem 的节奏，不改变传送带 `speedMps`、路径长度或在途运动速度。旧存档中的单值 `dispatchIntervalSec` 和仓库级冷却值必须分别复制到三个端口后再继续运行，工厂数据 schema 升级为 `4`。

开放式货架采用 mastjie `glb/rack.glb` 作为 CC0 主体视觉。运行时在世界轴执行显式 `Y +90°` 朝向修正与非等比缩放，消除旋转后长宽缩放轴互换的问题；业务占地为 `8m × 2m`，可见尺寸约为 `7.2m × 5.4m × 1.8m`，实际长度约为修复前异常拉伸外观的一半。架体上按四层、双排、五列组合摆放 40 个 Kenney `box-small.glb` / `box-wide.glb`，每组箱体以运行时层板顶面标高为基准落位，形成真正置于层板上的满载陈列；这些箱体只表达货架用途和满载感，不等同于 Inventory 数量。货架不设入货口或出货口，不参与普通/跨层传送带吸附和自动入出库；仍支持 90° 旋转，旧存档中曾连接货架的线路在恢复时自动解除货架端关系。

货架中的有限库存采用无限堆叠规则：单个 Item 和货架总量均不设业务容量上限，用户可直接上架任意非负安全整数数量，不受 Item 的 `maxStackSize` 或货物仓库 1800 件容量限制。每个“货架 × Item”记录也可独立切换为无限，并在场景标签、属性浮窗和物流页以 `∞` 明示；该设置表达货架记录无需维护有限数量，不会让无端口货架创建 TransitItem。实现层允许用 `Number.MAX_SAFE_INTEGER` 作为可持久化的无上限容量哨兵，但界面与业务语义必须显示为 `∞`，不得把该哨兵展示为实际容量或伪造库存。

例如：

```text
Warehouse A-07

A01：
钢板 ×30

A02：
钢板 ×48

A03：
电机 ×12

B01：
轴承 ×80
```

---

# 23. 自动入库和出库

系统为货物仓库与货架建立同一套库存记录生命周期，但只允许货物仓库参与传送带仓储逻辑。创建业务 Item 后，每个已放置仓库和货架自动建立该 Item 的零库存记录；放置仓储设施时也会为当前已有 Item 建立零库存记录。空白项目仍不注入任何默认 Item 或默认库存。

例如：

```text
Machine 出货口
↓
Conveyor
↓
Warehouse 入货口
```

或者：

```text
Warehouse 出货口
↓
Conveyor
↓
Machine 入料口 / 另一个 Warehouse 入货口
```

入货物料沿完整传送带路径运动，到达货物仓库或机器的输入锚点后才写入 Inventory 或输入缓存；货物仓库容量不足时，物料保持在传送带末端阻塞，不得凭空消失。仓库有限库存出货时，按每条出货传送带的端口配置从 Inventory 扣减一件，再创建对应 TransitItem。仓库仅有一种可供应物品时自动选择；存在多种可供应物品时，每条已连接出货带分别选择物品，未选择或所选有限库存为零时该通道不出货。三个出货口按各自间隔与冷却并行工作，某一端口等待不得冻结其他端口，所有有限库存运输必须满足物料守恒。货架库存可通过属性浮窗或物流页维护，也可由 AGV 实际取货或存货，但不进入上述传送带输送流程。

库存预约必须区分两个方向：`reservedOutboundQuantity` 只表示 AGV/无人机尚未装货但已锁定的起点现有货物，仓库和货架的可出货量按 `quantity - reservedOutboundQuantity` 计算；`reservedInboundCapacity` 只表示运输任务为终点预占的未来入库容量，货物仓库剩余容量按所有物品的 `quantity + reservedInboundCapacity` 汇总计算。入库容量预约不得减少终点已有货物的传送带可出货量，出库货物预约也不得在容量计算中把同一件现存货物重复占位。物流页应分别显示“出库预留”和“入库占位”，不再用含义混杂的单列“预留”。旧存档中的 `reservedQuantity` 必须依据活动中的 AGV/无人机运行时起点记录和终点记录重新构建两个方向的预约；无法与活动任务对应的旧值归零，不得盲目复制到两个字段。

TransitItem 的场景模型必须通过其 `itemId` 找到 Item，再以 `Item.itemModelId` 解析 `assets/3d/core/items/v1/catalog.json` 中对应的 GLB。首版 36 个稳定 `modelId` 必须全部覆盖，不能只为少数示例物品硬编码路径，正常已定义物品不得显示为白色通用方块。只有未知 `modelId` 或 GLB 加载失败时才允许显示带黄色故障标记的中性回退货物，并保留可诊断状态。

每个“仓储设施 × Item”库存记录可以显式切换为无限。货物仓库的无限供应是用户主动配置的仿真源边界：该物品可持续创建出货 TransitItem，不扣减实际数量、不占用额外有限库存容量；无端口货架的无限设置不会自行创建传送带运输，但在货架被配置为 AGV 起点时可作为显式无限源，装货不扣减实际数量。两类设置均在场景标签、属性浮窗和物流库存表中以 `∞` 明示，关闭后恢复显示并维护原有有限数量。无限状态不得用超大伪数量实现，也不得伪装成满足物料守恒的普通库存。

手动库存调整可直接在仓库或货架属性浮窗完成，并在“物流与仓储”页面保留集中管理入口，二者使用同一 Store 门禁。仿真空闲时设置的有限数量同时作为重置基线；运行中或暂停时的人工调整只改变当前数量。删除货物仓库时，其连接传送带、在途物料和所属库存记录一并清理；删除货架时清理所属库存记录。删除 Item 时只有在未被配方、非零库存或无限配置引用时才允许，并清理无引用记录。

机器需要原料时可以自动产生：

```text
Material Request
```

物流系统自动寻找库存并创建运输任务。

---

# 24. AGV 无人运输车辆

ForgeCore 支持可编程、可运行并参与真实库存结算的自动物流运输车辆。AGV 业务实体与 Cels 第三方视觉模型严格分离：车辆路线、速度、载重、安全包络、任务、库存预约和多车通行权全部来自 ForgeCore 数据，不从 vendor 网格推断。

当前 AGV 采用 `4m × 4m` 网格占地；Cels 主体子树相对原编辑器显示整体放大一倍，目标视觉包围尺寸为约 `3.5m × 2.1m × 2.9m`（左右 × 上下 × 前后）。建造预览、运行场景、选择框、碰撞占地与旧存档迁移必须同步使用该尺寸契约。视觉层仍为 `vendor-only / derived pending`，不得因业务仿真已实现而标记为 derived-ready。

AGV 可以负责：

```text
原材料运输

半成品运输

产品运输

仓储补货

机器补料

成品入库
```

AGV 属性：

```text
AGV ID

位置

速度

最大载重

最大容量

当前货物

电量

当前任务

目标位置

当前路线
```

点击场景中的某台 AGV 后，属性浮窗提供该车自己的可视化运输程序。程序以“起点 → 货物 → 终点”节点流显示，允许设置：

```text
仅 1F 的起点仓库或货架

每趟运输的物品与件数

仅 1F 的终点仓库或货架

一直运输

或监测起点/终点中该物品的数量，在达到或超过/达到或低于阈值时启动

车辆速度与业务最大载重
```

程序、运行状态、剩余路线、车载货物、完成趟数和阻塞原因必须可见并可保存。场景必须为当前可见楼层内所有已产生路线的 AGV 绘制实时规划轨迹，而不是只有选中车辆才显示：轨迹从车辆当前逻辑位置直接连接下一个尚未到达的 A* 节点，再沿剩余节点绘制，严禁回接刚刚经过的上一个节点，动态重规划或让行时立即替换；深色底线、彩色虚线、沿途箭头、路径节点与终点环全部采用半透明材质，既保证在灰白网格上可辨识，也不得以实色遮挡设备和货物；彩色虚线相位和沿途箭头都沿“车辆当前位置 → 目标点”方向向外流动，所选车辆的“取货点/卸货点/让行点”标签表达目标。黄色表示正常运输路线，红色表示受阻，蓝色表示协调让行；选中车辆的轨迹仅通过加粗和较高但仍为半透明的透明度高亮，其他车辆以更低透明度保持可见。没有实际任务路径时不得伪造车辆正在运行的路线。AGV 装货后在车体上显示所选 Item 的真实核心模型或参数化变体及车载数量。

---

# 25. AGV 自动取货

当前直接编程链路以 1F 的仓库或货架作为同层装卸站点；AGV 不得在 2F 及更高楼层放置或执行运输，也不承担跨层任务。货架与货物仓库使用同一套“仓储设施 × Item”库存事实，因此货架既可以作为起点被 AGV 实际扣减库存并取货，也可以作为终点接收卸货并增加库存；货架仍不提供传送带端口，其对外运输可以通过 AGV 或无人机。程序启动时先检查触发条件、起点可用库存、物品质量、车辆最大载重和终点剩余容量，再按每趟件数创建任务。有限库存任务必须预约起点数量和终点容量，防止传送带、手工库存调整或其他运输载具抢占同一批货物；货架的有限数量允许无限堆叠，作为终点时不受货物仓库 1800 件总容量限制。无限供应保持显式仿真源语义，不扣实际数量。

例如程序配置：

```text
起点：Rack A-12

终点：Warehouse B-03

Item-024 ×10

触发：起点数量达到或超过 30
```

系统：

```text
检查触发条件、载重和终点容量

↓

预约起点库存与终点容量

↓

AGV 前往起点

↓

实际扣减有限库存并装载车载货物

↓

沿安全最短路径运输到终点

↓

卸货并写入终点实际库存
```

装货到卸货之间，物料只存在于 `AgvRuntimeState.cargoQuantity`，库存与车载量之和必须守恒。删除载货车辆时应优先退回原有限库存并释放预约；重置仿真恢复初始库存并清空预约和车载状态。后续机器自动补料可以在相同任务协议上增加机器装卸适配器，不应另建平行运输系统。

---

# 26. AGV 自动寻路

工厂使用规则网格。

AGV 采用：

> **A\* Pathfinding**

进行路径规划。

地图：

```text
□ □ □ ■ □
□ ■ □ ■ □
□ ■ □ □ □
□ □ □ ■ □
```

其中：

```text
□ 可通行区域

■ 障碍物
```

AGV 输入：

```text
Start

Goal

Grid Map
```

输出：

```text
Shortest Path
```

当前实现使用八方向规则网格 A*：横纵相邻节点代价为 `1`，斜向相邻节点代价为 `√2`，以八角距离（Octile Distance）作为可采纳启发式，因此在同一离散可通行图上优先得到包含安全斜线的真正最短路径。每次斜向移动必须同时验证其两侧正交邻格均可通行，禁止贴着障碍角落切角或斜穿安全包络。导航节点不得越出工厂边界；机器、仓库、货架、缓冲区和传送带均为静态障碍。放大后的车辆使用独立 `2m` 保守半安全包络，传送带在自身约 `0.5m` 半宽基础上再叠加车辆包络；路径终点不是设施中心，而是 A* 在设施四周全部可达装卸候选点中选择的最短安全停靠点。该安全包络属于业务导航参数，不是从第三方模型包围盒反推的碰撞体。

AGV 的导航空间固定为 1F 平面。建造层不是 1F 时，建造命令必须直接拒绝 AGV；旧存档或异常数据中的上层 AGV 程序必须显示不可运行原因，不得把上层坐标悄悄投影回 1F。

路径在 `0.25s` 固定仿真步长内按明确的 `m/s` 前进，车辆位置、朝向、行驶距离、阻塞时间、车载货物和任务阶段均进入可保存的仿真状态；显示层在逻辑位置之间逐帧平滑，不得瞬移、斜穿障碍或只播放无库存结算的动画。

---

# 27. AGV 多车调度

当前多车协调已实现：

```text
实时车辆占用检测

最小 3.8m 车心安全间距

静止或慢行车辆作为动态障碍实时重规划

确定性固定通行权

低优先级车辆自动寻找让行点

让行完成后重新规划原任务最短路

迎面冲突与交叉口冲突的边交换禁止
```

车辆发现当前路线被另一台静止、等待或慢行车辆占用时，先把其他车辆的当前位置及短时运动包络写入动态障碍图，并从当前位置到本次装卸目标重新执行八方向 A*；存在安全绕路时立即替换剩余路线并继续任务，不能因为原路线被占就永久停车。只有当前安全图确实没有绕路，或两台运动中车辆发生迎面/交叉冲突时，才进入协调等待与让行。已经处于安全间距内的历史状态只允许执行能持续增大车距的脱离动作，避免碰撞门禁反过来锁死正在离场的车辆。

两车发生无法直接绕开的路线冲突时，按稳定车辆 ID 决定唯一通行权，只有低优先级车辆进入让行状态，从而消除“两车同时礼让、随后同时前进”的对称活锁。高优先级车辆在低优先级车辆未退出安全间距前仍必须停车，不能以通行权为由穿模；低优先级车辆使用动态安全图寻找离冲突车辆及其近期路径足够远的最近可达让行点，完成让行后重新执行原任务。若物理布局没有任何安全绕路或让行点，系统保持确定性阻塞并显示原因，不允许碰撞穿越。

货架与货物仓库的 AGV 装卸区域按设施级单机服务位协调，即使 A* 在设施不同侧找到多个可达停靠格，也不得让两辆车同时装卸同一设施。多车前往同一装卸设施时，系统按“装卸区内现有车辆或正在离场车辆优先，其次剩余路线更短，最后稳定车辆 ID”确定唯一进场权；其他车辆停在设施安全等待半径之外并显示共享装卸位排队原因。完成卸货的 AGV 不得永久停在装卸格等待库存触发，而应沿刚刚通过且已经过碰撞验证的路线反向退出到安全距离，再进入下一任务或等待状态。该进场仲裁、离场优先和安全退让同时覆盖货架与货物仓库，必须保证两辆 AGV 向同一设施取货或存货时依次完成而不死锁。

后续可以继续加入：

```text
任务优先级编辑

长时间窗道路预约

拥堵热力图

任务重新分配

智能充电

交通管制
```

例如：

```text
AGV-A
→

←
AGV-B
```

系统可以根据规则决定：

```text
等待

绕路

优先通过
```

从而模拟真实智能物流环境。

---

# 28. 无人机运输系统

无人机与 AGV 采用完全不同的运输机制。

无人机：

```text
可以三维移动

不依赖地面道路

可以跨越机器

可以跨楼层

运输速度高

载货量较低
```

例如：

```text
1F Machine A

        ↗

      Drone

             ↘

            4F Machine B
```

因此无人机适合处理：

```text
高价值零件

紧急补料

小批量运输

跨楼层物料
```

无人机模型只承担可视外形、空间包络和经审计的语义节点，不是载荷、速度、电池、任务或运输规则的事实来源。`Drone` 与 `TransportTask` 保存业务能力和运行状态，`VehicleModel` 保存视觉资产、坐标、碰撞、LOD、动画和来源许可；不得从文件名或模型外观推断真实飞行性能。

无人机使用与 AGV 完全一致的可视化任务编程：逐机设置起点仓库或货架、终点仓库或货架、物品、每趟件数、持续运输或起点/终点库存阈值、速度和业务最大载重；区别只在可选装卸点覆盖全部楼层，且默认载重与单趟运量明显低于 AGV。货物仓库作为起点时，无人机必须飞到仓库出货口外侧装货；货物仓库作为终点时，必须飞到入货口外侧卸货。三个同侧端口均作为候选装卸航点，按仓库旋转后的真实端口方向生成，并由三维 A* 在满足建筑安全净空的候选中选择可达最短点；不得继续把仓库中心正上方作为固定装卸点。货架没有输送端口，继续使用架体上方的安全装卸点。任务仍执行库存触发、起点与终点预约、真实扣减、车载货物、真实卸货与总量守恒，不能只播放飞行动画。

无人机导航采用规则三维体素 A*。每个节点保存世界坐标 `x/y/z`，相邻集合覆盖立方体的 26 个方向：单轴直线移动代价为 `1`，两个轴同时变化的面对角移动代价为 `√2`，三个轴同时变化的体对角移动代价为 `√3`；启发式使用对应的三维对角距离。任何面对角或体对角移动都必须检查所跨越的全部轴向/边向相邻体素，禁止切过建筑棱角。工厂 X/Z 边界、最低安全高度和最高楼层上方的飞行上限共同限制搜索空间。

机器、货物仓库、货架与缓冲区按其所属楼层标高、业务占地、已登记视觉高度和无人机独立安全包络构成三维静态障碍；传送带、跨层传送带和 AGV 低于无人机安全飞行高度，不写入无人机障碍图。无人机可在有净空时从建筑上方飞越，但不得穿过建筑体积。跨层向下飞行时，下降边只允许发生在整段垂直柱体均无遮挡的位置：无人机必须先横向或斜向飞到安全净空列，再垂直下降到下一飞行高度，禁止直接穿楼层建筑或贴着障碍斜切下降。向上飞行与其他三维绕障仍可使用面对角和体对角最短移动。

无人机之间使用三维球形安全间距、短时位置预测和动态障碍重规划。候选移动若侵入另一架无人机的安全球，系统按稳定载具 ID 决定唯一通行权：低优先级无人机先把高优先级无人机及其近期位置写入动态障碍图并重新执行三维 A*，确实无路时才等待；高优先级无人机也不得穿过正在让行的机体。已经处于安全间距内的历史状态只允许执行能持续增大双方距离的脱离动作，不得因碰撞门禁把正在离场的无人机再次锁死。该规则必须消除双方同时礼让的对称活锁，并保持确定性仿真。

货架上方以及货物仓库入货口/出货口外侧的无人机装卸区域采用设施级单机服务位。同一时刻有多架无人机飞往同一仓储设施时，系统按“装卸区内现有无人机优先离场，其次剩余航程更短，最后稳定载具 ID”确定唯一进场权；其余无人机必须停在装卸区安全等待半径之外，状态显示正在等待共享装卸位，不得因为选择了不同编号端口就同时挤入同一仓库。完成装货或卸货的无人机不得永久停在装卸点：若当前运输循环已经结束，必须沿刚刚通过且已验证无静态障碍的进场航路反向退出到安全等待距离之外，再进入库存触发等待。该终点仲裁、离场优先和安全退让必须同时覆盖货架与货物仓库的输入/输出侧，并保证两架无人机向同一设施存货或取货时均能依次完成而不死锁。

场景显示无人机的实时三维规划轨迹、方向箭头、节点、终点标记、取货点/卸货点标签和剩余三维距离；路线底线、状态虚线、箭头、节点与终点标记全部采用半透明材质，选中路线只提高透明度但不得恢复为近乎不透明，避免三维路线遮挡建筑、货物和其他无人机；路径重规划后立即替换。无人机本体、车载真实 Item 模型和轨迹均以绝对楼层标高换算到当前视图，跨层飞行过程中不得瞬移、贴地穿模或在切换楼层时跳回建造坐标。

---

# 29. 物流任务系统

所有自动运输统一抽象为：

```text
Transport Task
```

例如：

```text
Task ID：
LOG-00071

Source：
Rack-A12

Target：
Machine-07

Item：
MAT-004

Amount：
20

Priority：
HIGH
```

系统根据任务自动选择：

```text
AGV

Drone

Conveyor
```

等运输方式。

---

# 30. 工厂生产网络

整个数字工厂在后台可以抽象成一个：

> **有向生产物流图**

例如：

```text
Warehouse
   ↓
Machine A
   ↓
Machine B
   ↓
Splitter
  ↙       ↘
C         D
↓         ↓
Rack      Warehouse
```

后台表示为：

```text
Node

Edge
```

节点可以是：

```text
Machine

Rack

Warehouse

Buffer

Splitter

Merger

Elevator
```

边可以是：

```text
Conveyor

AGV Route

Drone Route
```

这一抽象非常适合后续进行生产分析和 AI 优化。

---

# 31. 工厂仿真系统

系统需要存在完整的生产仿真引擎。

仿真系统负责：

```text
物品生成

机器加工

生产等待

传送带运输

仓储变化

AGV运输

无人机运输

生产堵塞

产能计算

时间推进
```

---

# 32. 工厂时间系统

用户可以控制仿真速度。

例如：

```text
Pause

1×

2×

5×

10×

50×
```

用户因此可以快速观察：

```text
未来10分钟

未来1小时

未来8小时

未来1天
```

工厂运行结果。

---

# 33. 实时数据采集

系统需要持续记录工厂运行数据。

例如：

```text
Machine Throughput

Machine Utilization

Machine Idle Time

Conveyor Utilization

AGV Utilization

Transport Time

Inventory

Production Output

Queue Length

Block Time
```

这些数据提供给：

```text
Dashboard

Agent 分析

生产优化
```

使用。

---

# 34. 工厂实时监控中心

系统提供 Dashboard。

主要指标：

```text
当前生产速度

单位时间产量

机器利用率

当前在制品数量

生产线堵塞数量

AGV利用率

无人机利用率

平均运输时间

库存水平

生产任务完成率
```

---

# 35. 机器利用率分析

例如：

```text
Machine A

运行：
72%

等待输入：
18%

输出堵塞：
10%
```

系统通过这些数据判断：

```text
为什么机器没有达到理论产能
```

---

# 36. 生产瓶颈检测

系统可以自动检测生产瓶颈。

例如：

```text
Machine-01：
产能 100/min

Machine-02：
产能 95/min

Machine-03：
产能 42/min

Machine-04：
产能 110/min
```

系统判断：

```text
Machine-03
```

是整条生产线主要瓶颈。

---

# 37. 物流瓶颈检测

生产能力也可能被物流限制。

例如：

```text
机器最大产能：

100件/min

实际产量：

62件/min
```

系统发现：

```text
Machine 没有满载

↓

传送带频繁缺料

↓

AGV运输平均耗时过长
```

因此真正瓶颈可能是：

```text
物流系统
```

而不是生产机器。

---

# 38. Agent 工厂分析系统

Factory Agent 可以通过只读工具读取：

```text
生产数据

设备数据

运输数据

库存数据

工厂结构

配方关系

生产网络
```

并回答用户问题。

例如：

```text
为什么当前生产效率只有70%？
```

Agent 可以基于结构化证据分析：

```text
Machine-05利用率达到98%

Machine-06平均等待输入时间达到21%

AGV运输区域A存在明显拥堵

Rack-03库存不足
```

最后生成：

```text
主要瓶颈：

Machine-05

次要瓶颈：

区域A物流系统
```

---

# 39. Agent 生产优化与行动建议

Agent 可以针对工厂状态提出结构化调整方案，但建议必须经过规则校验和分支仿真后才能申请执行。

例如：

```text
当前：

产量 720件/h
```

Agent 候选方案：

```text
增加一台Machine-05

将两条输入传送带合并

增加一个Buffer

AGV数量从3增加到4
```

预计：

```text
产量：

910件/h

提升：

26.4%
```

---

# 40. Agent 物流优化与调度

Agent 可以分析：

```text
AGV路线

运输距离

交通拥堵

任务等待

货架位置
```

然后建议：

```text
将Rack-A移动至Machine-03附近

减少AGV平均运输距离

重新规划主运输道路

增加一台AGV

部分高频物料改用传送带运输
```

---

# 41. Agent 生产方案分支仿真

Agent 提出优化方案后，不直接修改用户工厂。

系统首先创建：

```text
Simulation Branch
```

即当前工厂的临时仿真副本。

在副本中应用结构化 Factory Patch。

例如：

```text
当前工厂

↓

复制

↓

Agent 应用 Patch

↓

模拟8小时

↓

统计结果
```

最终比较：

```text
Current Factory

Production：
720/h

AI Proposal

Production：
915/h

Improvement：
+27.1%
```

用户可以选择：

```text
应用方案

放弃方案

进一步优化
```

---

# 42. ForgeCore Factory Agent 自动优化器

长期版本可以实现：

> **Factory Agent 自动工厂优化器**

用户输入目标：

```text
产品：
Product-A

目标产能：
120/min

最大面积：
60×60

最大楼层：
3

最大AGV：
8
```

Agent 通过规则、求解器和仿真搜索：

```text
机器数量

机器布局

生产线路

仓储位置

传送带结构

AGV配置
```

并生成多个方案。

例如：

```text
方案A

最低成本
```

```text
方案B

最高产量
```

```text
方案C

最低占地
```

```text
方案D

物流效率最高
```

---

# 43. 工厂蓝图系统

用户可以保存完整工厂布局。

保存为：

```text
Factory Blueprint
```

蓝图包括：

```text
楼层

机器

位置

配方

传送带

货架

AGV

无人机

物流规则
```

用户可以：

```text
保存

复制

导入

导出

重新加载
```

长期甚至可以实现：

```text
Blueprint Community
```

允许用户分享数字工厂方案。

后端 B6/B7 已提供蓝图的持久化与社区 API：蓝图保存的是不含运行时状态的完整设计快照，支持 owner 读取/更新/删除、公开列表、搜索、标签筛选、最近/热门排序、收藏、fork、fork 记录和 `.fcbp` JSON 导入导出。fork 会重新映射工厂、楼层、对象、物品、配方、库存及嵌套配置引用，不能复用原蓝图的业务 ID。当前前端仍以工厂编辑器为主，独立的蓝图社区工作台属于后续页面接入。

---

# 44. 模块化设施系统

所有设施采用模块化设计。

例如：

```text
FactoryObject

├ Machine
├ Conveyor
├ Rack
├ Buffer
├ Splitter
├ Merger
├ Elevator
├ AGV
└ Drone
```

未来增加新的设施时不需要修改整个系统。

例如可以继续加入：

```text
机械臂

自动包装机

质量检测机

自动码垛机

叉车

智能仓库

管道运输

液体储罐
```

---

# 45. 推荐数据库实体

主要实体可以包括：

```text
User

Factory

Floor

FactoryObject

Item

ItemModel

Recipe

RecipeInput

RecipeOutput

Machine

MachinePort

Conveyor

Splitter

Merger

Buffer

Rack

RackSlot

Inventory

AGV

Drone

VehicleModel

TransportTask

ProductionTask

Simulation

SimulationRecord

AIAnalysis

AIOptimizationPlan
```

其中 `Item` 与 `ItemModel` 必须分库存储、通过稳定 `modelId` 关联：

```text
Item
├ id / code / name / category / description
├ itemModelId -> ItemModel.modelId
├ modelParameters
├ icon
├ massKg
└ maxStackSize

ItemModel
├ modelId / category / libraryVersion
├ relativePath / previewPath
├ parameterSchema / defaultParameters
├ materialSlots
├ bounds / geometryMetrics
└ sha256 / sourceType
```

`ItemModel` 首版数据来自 `assets/3d/core/items/v1/catalog.json`。`Item.modelParameters` 只允许保存 catalog schema 中声明且由用户明确设置的参数覆盖；`massKg` 和 `maxStackSize` 仅影响物流、库存、堆叠与仿真业务，不进入模型生成参数。模型库版本升级时保留已被 Item 引用的 `modelId` 稳定性，破坏性替换必须提供迁移映射。

AGV 与 Drone 同样把业务实体和视觉模型分离：

```text
Drone / AGV
├ id / code / name / factoryId
├ vehicleModelId -> VehicleModel.modelId
├ transform / currentFloorId / status
├ maxPayloadKg / maxCargoVolume / speed / verticalSpeed
├ batteryCapacity / batteryLevel
└ currentTransportTaskId

VehicleModel
├ modelId / vehicleType
├ vendorSourcePath / derivedRuntimePath / sourceSha256
├ unitScale / upAxis / forwardAxis / pivot
├ semanticNodeMap / animationBindings
├ collisionProfile / navigationSafetyEnvelope / lodVariants
└ provenance / license / attribution / modificationRecord
```

`vendorSourcePath` 只用于追溯原件；正式仿真加载 `derivedRuntimePath` 指向的审计通过版本。飞行载荷、速度、电池和任务能力属于 `Drone`，不得写进 `VehicleModel` 或从网格推断。

---

# 46. 核心关系

```text
Factory
│
├ Floor
│
├ FactoryObject
│
├ Item
│
├ Recipe
│
├ TransportTask
│
└ Simulation
```

Machine：

```text
Machine
│
├ Recipe
├ InputPort
├ OutputPort
└ Buffer
```

Recipe：

```text
Recipe
│
├ Input Item
└ Output Item
```

Item 与视觉模型：

```text
Item N ── 1 ItemModel
  │          ├ parameterSchema
  │          ├ defaultParameters
  │          └ GLB + Preview + SHA-256
  └ modelParameters（单个 Item 的参数覆盖）
```

多个 Item 可引用同一 `ItemModel`；在结构参数相同时共享几何和实例化渲染，只在结构参数组合变化时从定义重建网格。质量、堆叠数、库存量和工艺关系不参与模型生成，也不得反向写入 `ItemModel`。

运输设备与视觉模型：

```text
AGV   N ── 1 VehicleModel
Drone N ── 1 VehicleModel
                 ├ vendor 原件与来源 SHA-256
                 ├ derived 运行时 GLB
                 ├ 碰撞体 / 安全包络 / LOD
                 └ 语义节点与动画绑定
```

设施端的 `pickup`、`dropoff`、`landing` 和 `approach` 锚点归属于机器、货架或停机位；无人机模型只保存自身的 `bodyRoot`、`navigationOrigin`、`cargoMount` 以及经资产验证存在的可动部件映射，避免把运输两端语义错误地固化到载具网格中。

---

# 47. 推荐技术栈

## 前端

推荐：

```text
Vue 3 / React

TypeScript

Vite

Three.js

ECharts

Pinia / Zustand
```

其中 Three.js 主要负责：

```text
3D场景

工厂模型

网格

摄像机

设备模型

物品动画

AGV移动

无人机移动

楼层切换

编辑交互
```

### 3D 工厂素材基线

ForgeCore 将 3D 资产明确分为 `core`、`vendor` 与 `derived` 三类：

- `assets/3d/core/`：ForgeCore 原创、可重现的 first-party 运行时资产，不是 vendor 原包，也不是第三方模型的派生件。
- `assets/3d/vendor/`：原样保留的第三方上游资产，以许可和来源可追溯为目标。
- `assets/3d/derived/`：对 vendor 资产进行减面、尺度、材质、碰撞体、LOD 或语义化后的项目派生件，必须保留来源关系和许可要求。

GitHub 仓库分发使用 Git LFS 保存不适合普通 Git 差异存储的二进制 3D 与预览资产；`.gitattributes` 对 `*.glb`、`*.fbx`、`*.obj`、`*.png` 和 `*.jpg` 启用 LFS，源码、配置、文档及轻量文本资产继续使用普通 Git。克隆者需要启用 Git LFS 才能取得完整模型和图片；LFS 只改变仓库存储与传输方式，其指针不得被当成资产内容、不得改变 `catalog.json`、`SHA256SUMS` 或 `ASSET_AUDIT.md` 中针对实际文件的校验值，也不得借此覆盖或改写 `core`、`vendor`、`derived` 的来源与许可边界。

#### ForgeCore 首版默认物品模型库（first-party/core）

`assets/3d/core/items/v1/` 内置 36 个参数化低模物品，分为 basic、material、mechanical、electronic 和 package 五类。它们由 `tools/item-models/` 中的 ForgeCore 原创零依赖生成器生成，不包含也不派生于第三方网格或纹理。运行时契约为自包含 GLB 2.0、米制、Y-up、ground-center，资产完整性由 `catalog.json` 与 `SHA256SUMS` 共同审计。

生成与验证只需要 Node.js，无 npm 包或外部建模工具依赖：

```powershell
node tools/item-models/build.mjs
node tools/item-models/validate.mjs
```

#### 第三方视觉资产（vendor）

ForgeCore 当前采用四套可追溯的第三方资产作为工厂、仓储、AGV 与货运无人机视觉资源：

1. **Kenney Factory Kit 3.0**：编辑器默认工厂设施、机器与流水线模型库，项目内权威路径为 `assets/3d/vendor/kenney-factory-kit-3.0/`，Web 端优先使用 `Models/GLB format/`。
2. **mastjie Low Poly Warehouse Kit**：提供开放式货架主体与其他仓储场景原件，项目内权威路径为 `assets/3d/vendor/mastjie-low-poly-warehouse-kit/`；`glb/rack.glb` 已作为底部建造栏独立“货架”的单实例视觉主体，并组合 Kenney 箱体形成满载陈列。封闭式货物仓库继续使用 Kenney 候选 F，mastjie 的 warehouse、crate、container、barrel 原件保持原样且暂未接入运行场景。
3. **Cels Industrial 3D AGV Trolley**：补充 AGV 无人运输车视觉资产，原始 GLB 位于 `assets/3d/vendor/cels-industrial-agv-trolley/industrial_3d_agv_trolley_free_low-poly_3d_model.glb`。
4. **Count Infinity Futuristic Delivery Drone**：补充货运无人机视觉资产，原始 GLB 位于 `assets/3d/vendor/count-infinity-futuristic-delivery-drone/futuristic_delivery_drone.glb`，署名记录位于同目录 `ATTRIBUTION.md`。

Kenney 与 mastjie 资产采用 **Creative Commons Zero（CC0）**。Cels AGV 与 Count Infinity 无人机均采用 **Creative Commons Attribution 4.0（CC BY 4.0）**。Cels AGV 使用和分发时必须注明作品名称、作者 Cels、[Sketchfab 原模型页](https://sketchfab.com/3d-models/industrial-3d-agv-trolley-free-low-poly-3d-model-016a066401a3489dafa98669d3a4b3f1)及 [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)；无人机必须注明作品名 Futuristic Delivery Drone、作者 [Count Infinity](https://sketchfab.com/countinfinity)、[Sketchfab 原模型页](https://sketchfab.com/3d-models/futuristic-delivery-drone-48d0997629ee4cc9836775a523651017)及 CC BY 4.0。两者的派生版本都必须说明修改内容。GLB 作为 Web 3D 运行时首选格式；FBX 与 OBJ（若上游提供）仅作为建模软件交换、二次加工和兼容性备份。

上游原始文件必须保持只读式管理，不在 vendor 内直接覆盖、改名或制作项目定制版本。mastjie 货架已通过容器、几何、性能、依赖和离线外观检查；原始尺度约为 `4.9777 × 9.5955 × 19.9950`（X × Y × Z，模型单位），当前编辑器只把它作为经显式 `Y +90°` 轴向修正、在世界轴缩放到约 `7.2 × 5.4 × 1.8m` 的单实例视觉层，业务占地、碰撞和库存均来自 ForgeCore 数据；货架不定义输送端口。当前原件没有 LOD、碰撞体或货位语义节点；若进入规模化实例、物理碰撞、RackSlot 寻址或导航仿真，仍必须在 `assets/3d/derived/` 中完成尺度固化、材质统一、LOD、简化碰撞体与语义挂点，并记录与上游 `glb/rack.glb` 的来源关系。

Cels AGV 原始场景已通过容器、几何、依赖、来源许可和离线外观检查。AGV 主体约为 `1.0560 × 0.8884 × 0.8108`（X × Y × Z，模型单位），但原文件还包含两层输送机构、纸箱和约 `41 × 41` 的展示地板，全场景约 7.36 万三角面，且没有动画、LOD、碰撞体或导航语义节点。正式接入仿真前，必须在 `assets/3d/derived/` 中剥离展示地板、重新居中定轴、确认米制尺度、减面与合并材质、生成 LOD/简化碰撞体，并配置车体、轮组、载台和导航锚点等 AGV 语义层。

Count Infinity 无人机原件为自包含 GLB 2.0，共 3 个网格/图元、12,436 个三角形和 3 个 PBR 材质，内嵌 10 张 1024×1024 PNG，无外部 URI、动画、蒙皮、相机或 glTF 扩展。离线外观检查确认其为带大型下挂货舱、封闭圆形推进舱和翼面的未来货运无人机，未发现展示底座、明显缺面、错位、异常穿插或裁切。根节点已旋转为 Y-up，但文件未声明单位、前向轴和业务尺度；总包围盒约为 `33.318 × 58.059 × 52.500` 原始单位，不能直接按米制解释。其 `Cargo`、`Drone`、`Flaps` 只是合并网格，原件没有可独立驱动的旋翼/襟翼节点。当前 Web 单实例视觉适配在不修改 vendor 文件的前提下额外施加 `Y +180°` 固有旋转，使模型可见机头与 ForgeCore 载具航向一致；建造预览和运行实体必须共用该旋转常量，业务航向、路径和碰撞数据不随视觉修正改变。正式接入仿真前，必须在 `assets/3d/derived/` 中确认米制尺度和 +Z 前向，保留合适的飞行枢轴，压缩或降采样贴图，生成 LOD、简化碰撞体与独立导航安全包络，配置 `bodyRoot`、`navigationOrigin`、`cargoMount`，并把不存在的动画明确标记为 unavailable 或登记为程序驱动。vendor 原件技术上可单实例加载，但不作为规模化无人机运行时资产。

详细版本、许可证、逐文件清单、SHA-256 和导入验证记录见：`assets/3d/ASSET_AUDIT.md`。

---

# 48. 后端

ForgeCore 当前采用 **Python + FastAPI + SQLAlchemy 2 + Alembic** 的单一业务后端，避免在 MVP 阶段同时维护 Java 和 Python 两套权限、数据模型与 API。前端仿真引擎仍以 TypeScript 在浏览器运行；后端负责身份、工厂设计快照、仿真状态/历史、蓝图和 Agent 编排，预留未来批量仿真或优化服务的边界。

```text
React / Three.js 前端
        │ HTTPS JSON + SSE
        ▼
FastAPI API
  ├─ Auth：JWT access/refresh、当前用户、登出
  ├─ Factory：创建、列表、读取、删除、全量同步
  ├─ Simulation：状态、指标、活动持久化
  ├─ Blueprint：保存、公开发现、收藏、fork、.fcbp
  ├─ Agent：Redis 会话、结构化 Suggestion、事件流
  └─ Realtime：Redis Pub/Sub → SSE
        ├──────────────┐
        ▼              ▼
 PostgreSQL         Redis
 设计与历史数据       会话、事件、撤销令牌
```

后端代码位于 `backend/`，配置模板为 `backend/.env.example`。数据库迁移必须通过 Alembic 执行，开发 Compose 默认使用 PostgreSQL `5440` 和 Redis `6380`，FastAPI 默认监听 `8010`。

---

# 49. 数据库

推荐：

```text
PostgreSQL
```

缓存和实时状态可以使用：

```text
Redis
```

后期大量时间序列数据可以考虑：

```text
TimescaleDB
```

---

# 50. 实时通信

工厂运行过程中存在大量实时数据。

例如：

```text
物品位置

AGV位置

机器状态

库存变化

产量变化
```

MVP 使用 **Redis Pub/Sub + SSE（Server-Sent Events）** 实现服务器到前端的实时同步。SSE 适合指标、活动、仿真状态和 Agent 进度这类单向事件；需要双向协作编辑时，再评估 WebSocket 或操作同步协议，不把实时传输层和持久化层混为一谈。

已实现的订阅地址：

```text
GET /api/realtime/factory/{factory_id}/events
GET /api/realtime/agent/{session_id}/stream
```

工厂保存、指标、活动和仿真状态更新会发布到对应 Redis 频道；事件流断开时前端保留已知状态，并在 HTTP 请求失败时按 repository 策略回退到本地副本。

---

# 51. ForgeCore Factory Agent 技术方案

ForgeCore 的 AI 能力正式定位为 **ForgeCore Factory Agent**。它不是附加在页面上的聊天助手，也不是只生成分析文字的报表工具，而是能够读取工厂事实、制定计划、调用受控工具、创建仿真分支、验证方案、请求审批、应用变更并复核结果的执行型 Agent。

完整闭环为：

```text
用户目标
→ 目标与约束结构化
→ 读取工厂事实
→ 制定可检查计划
→ 调用分析或设计工具
→ 生成 Factory Patch
→ 创建 Simulation Branch
→ 运行确定性仿真
→ 对比候选方案
→ 请求用户审批
→ 应用 Patch
→ 再次验证
→ 写入审计与可撤销记录
```

## 51.1 能力边界与职责分工

LLM 只负责难以用固定规则覆盖的语义工作：

- 理解自然语言目标、补齐需要用户确认的约束；
- 把复杂任务拆成计划并选择工具；
- 根据结构化证据提出候选策略；
- 解释诊断、差异、风险和仿真结果；
- 在多个可行方案之间说明取舍。

确定性模块负责可验证事实：

- 规则引擎负责端口、占地、碰撞、配方、库存、楼层、许可和业务约束；
- 统计分析负责产能、利用率、等待时间、WIP、缺料、堵塞和运输指标；
- 仿真引擎负责物料守恒、设备状态、物流结算和方案结果；
- 优化算法负责排产、任务分配、路径、布局搜索和多目标候选生成；
- Patch 系统负责唯一允许的设计修改、版本校验、反向操作和回滚。

LLM 不得直接写 PostgreSQL、Redis 或前端 Store，不得自行构造已经存在确定性工具可以计算的数字，也不得绕过业务校验把自然语言建议直接变成正式工厂状态。

## 51.2 Agent 工作模式

Agent 提供四种逐级开放的模式：

1. **只读分析**：默认模式，只查询事实、诊断原因和回答问题，不产生工厂变更；
2. **方案设计**：生成一个或多个结构化 Patch，在临时分支中仿真并比较，不修改主工厂；
3. **监督执行**：用户审批某个通过验证的方案后，由 Agent 应用、复核并提供一键回滚；
4. **策略托管**：高级模式，用户预先设置时间、成本、风险、对象范围和动作白名单，Agent 只在策略授权范围内持续监控和执行，超出预算或影响正式设计的动作仍必须重新审批。

任何新用户、新工厂和新 Agent 会话都从只读分析开始。策略托管不是无限权限，也不能成为绕过审批矩阵的后门。

## 51.3 高级功能范围

### 51.3.1 目标编译与约束协商

Agent 将“把产能提升到 120 件/分钟，面积和 AGV 数量不变”编译为结构化 `FactoryGoal`：目标指标、硬约束、软约束、优化时间窗、基线版本、允许动作和停止条件。目标存在冲突或不可行时，Agent 必须指出冲突并给出最小放宽集合，而不是静默修改用户要求。

### 51.3.2 因果诊断与证据链

诊断不只展示相关性，而要沿生产依赖图、库存时间线、机器状态、端口队列和载具任务追踪根因。每项结论都关联对象 ID、时间窗、指标和可复查证据，并区分“观测事实”“规则推导”“模型假设”和“仿真预测”。

### 51.3.3 自然语言建厂与局部改造

用户可以描述产品、目标产能、楼层、面积、成本和物流约束。Agent 先创建 Item、Recipe、设备数量和物流拓扑草案，再由布局与路径算法生成合法坐标，最后通过分支仿真验证。LLM 只生成语义计划和拓扑意图，不直接猜测网格坐标或传送带路径。

### 51.3.4 多方案分支与多目标优化

Agent 可以并行生成和评估不同方向的候选方案，例如最低成本、最高产量、最低 WIP、最低占地、最短运输距离和最高抗故障能力。结果以 Pareto 前沿展示，不把多个相互冲突的目标粗暴压成一个不可解释的总分。

### 51.3.5 生产与物流联合优化

Agent 同时考虑机器节拍、缓存、仓库端口、传送带吞吐、AGV/无人机载荷、任务等待、装卸点排队和跨层距离，避免只优化机器数量却把瓶颈转移到物流系统。高级版本支持订单优先级、批次、交期、换线成本和补料策略。

### 51.3.6 实时运营与异常处置

Agent 可订阅工厂指标和活动，识别缺料、堵塞、设备停机、库存异常、装卸点拥塞和订单延期风险。处置流程必须先给出影响面和候选动作；只读告警可自动执行，暂停仿真、重派任务和改变运行策略按授权级别执行，改变正式设计仍走 Patch 审批。

### 51.3.7 故障、维护与韧性推演

高级仿真支持注入机器故障、AGV/无人机不可用、仓库断供、路径封锁和处理时间波动。Agent 可计算单点故障、恢复时间、备用能力和安全库存，生成预防性维护窗口与应急方案。预测维护必须基于真实或模拟状态序列，不以 LLM 猜测故障概率。

### 51.3.8 成本、能源、质量与碳排目标

在基础产量和物流指标稳定后，为机器、载具、库存和物料增加成本、能耗、质量损失与排放模型。Agent 可执行多目标权衡，但每项结论都必须显示所用模型版本、单位、数据来源和未覆盖假设。

### 51.3.9 多工厂与供应链协同

长期版本将 Agent 作用域扩展到多个工厂、订单、供应商、运输时延和交付网络，支持产能分配、缺料传播、库存策略和供应中断场景。跨工厂动作必须由更高权限策略控制，并保留独立租户和数据边界。

### 51.3.10 可控记忆与经验复用

Agent 可以保存用户批准的目标偏好、常用约束、历史方案、失败原因和工厂术语。会话摘要不能成为事实来源；对象状态始终从当前工厂快照重新读取。长期记忆必须可查看、可删除、可禁用，并区分用户偏好、工厂事实和算法经验。

## 51.4 总体架构

```text
React Agent Control Room
        │ HTTPS + SSE
        ▼
FastAPI Agent API
        │
        ├─ Run Orchestrator：状态机、预算、取消、重试
        ├─ Context Builder：快照、指标、事件、依赖图
        ├─ Planner：目标编译、计划、工具选择
        ├─ Tool Registry：参数校验、权限和结果契约
        ├─ Policy Engine：动作分级、审批和风险门禁
        ├─ Patch Service：前置条件、版本、反向操作
        ├─ Branch Manager：分支隔离、生命周期和对比
        └─ Evaluator：规则、仿真、评分和复核
                 │
       ┌─────────┴──────────┐
       ▼                    ▼
Headless Simulation     Optimization Workers
共享 TypeScript 核心     Python 规则与优化算法
       │                    │
       └─────────┬──────────┘
                 ▼
        PostgreSQL + Redis
```

浏览器当前持有的确定性仿真逻辑必须逐步抽取为无 React、Three.js 和 DOM 依赖的共享 TypeScript 仿真核心，供前端和 Node worker 使用同一份规则。高级 Agent 不得依赖用户持续打开浏览器来完成批量方案仿真。Python 优化 worker 只生成候选参数和调用仿真，不再重新实现一套容易漂移的生产结算规则。

PostgreSQL 保存 Agent run、step、tool call、approval、patch、branch、evaluation 和审计记录；Redis 负责短期会话缓存、任务队列、分布式锁、取消信号和 SSE 发布。Redis Pub/Sub 只承担实时通知，不作为高级 Agent 的唯一事实来源。

## 51.5 专业能力协作

高级版本可以在同一 Orchestrator 下启用多个专业角色：

- Production Analyst：生产依赖、节拍、配方和瓶颈；
- Logistics Planner：传送带、仓储、AGV、无人机和装卸点；
- Layout Optimizer：楼层、占地、碰撞、接口和路径；
- Cost & Sustainability Analyst：成本、能源、库存和排放；
- Risk Critic：约束复核、反例、故障场景和结果可信度。

这些角色共享同一份带版本的事实快照，只提交证据或候选方案，由主 Orchestrator 仲裁。它们不能各自修改工厂，也不能通过“多 Agent 投票”替代规则校验和仿真证据。首版先实现单 Orchestrator 和确定性工具，专业角色在工具边界稳定后再启用。

## 51.6 工具注册表

只读工具可以在默认模式直接调用：

```text
get_factory_snapshot
get_factory_graph
get_simulation_metrics
query_event_timeline
inspect_bottlenecks
inspect_inventory
inspect_machine
inspect_recipe_chain
inspect_conveyors
inspect_logistics
calculate_capacity
explain_constraint
```

分支工具只能修改临时副本：

```text
propose_factory_patch
validate_factory_patch
create_simulation_branch
apply_patch_to_branch
run_branch_simulation
inject_failure_scenario
compare_branch_results
search_candidate_set
```

正式动作需要审批或策略授权：

```text
apply_factory_patch
rollback_factory_patch
pause_simulation
resume_simulation
update_dispatch_policy
reassign_logistics_tasks
```

每个工具必须声明 JSON Schema、只读或写入等级、超时、幂等规则、前置权限、可访问对象范围、返回证据和失败类型。Agent 只能调用注册工具，不能生成任意 SQL、任意 Python 或任意前端脚本执行。

## 51.7 核心数据契约

- `FactoryGoal`：目标指标、硬约束、软约束、时间窗、动作白名单和基线版本；
- `AgentRun`：用户目标、当前状态、模式、模型配置、预算、截止时间和取消状态；
- `AgentStep`：计划步骤、依赖、状态、重试次数和结果摘要；
- `ToolCall`：工具名、结构化输入、权限决策、输出引用、耗时和错误；
- `FactoryPatch`：基线版本、操作列表、前置条件、影响范围、风险等级、反向操作和幂等键；
- `SimulationBranch`：来源版本、临时设计、仿真配置、随机种子、状态和保留期限；
- `BranchEvaluation`：产量、WIP、等待、库存、距离、拥堵、成本、风险和约束满足情况；
- `ApprovalRequest`：待审批动作、差异、证据、风险、有效期和批准者；
- `AgentMemory`：明确分类、来源、作用域、有效期和删除状态的可控记忆。

`FactoryPatch` 必须使用稳定对象 ID 和显式操作，不使用自然语言 diff。每个操作都必须带前置条件和可生成的逆操作；应用时检查 `base_version`，主工厂已变化则停止并重新规划，不得覆盖用户在 Agent 运行期间完成的新修改。

## 51.8 Run 状态机

```text
created
→ contextualizing
→ planning
→ executing_tools
→ awaiting_clarification（按需）
→ awaiting_simulation
→ simulating
→ evaluating
→ awaiting_approval
→ applying
→ verifying
→ completed
```

任意可中断阶段都可以进入 `cancelled`，可恢复错误进入 `retrying`，不可恢复错误进入 `failed`，已应用方案在执行回滚后进入 `rolled_back`。状态转换由服务端持久化状态机控制，不能由前端事件或 LLM 文本任意改写。

## 51.9 优化算法路线

- 产能与配方网络先用图算法、线性约束和解析容量计算缩小搜索空间；
- 设备数量、任务分配、排产和订单选择优先使用 OR-Tools CP-SAT；
- AGV/无人机具体路径继续使用已有确定性 A*，高级调度在其上增加时间窗、冲突预约和任务分配；
- 布局采用合法构造器、网格约束、启发式邻域搜索和模拟退火或遗传搜索，禁止让 LLM 逐点放置；
- 多目标仿真优化采用 Pareto 搜索，可使用 NSGA-II 等算法，并保留种子、预算和每个候选的完整结果；
- 故障与供应波动使用固定场景集和可复现 Monte Carlo 运行，报告均值、分位数和最坏场景；
- LLM 负责提出搜索方向和解释取舍，不负责替代求解器或伪造最优性证明。

## 51.10 权限、安全与可靠性

- 默认只读，分支写入与正式写入严格隔离；
- 正式变更、批量删除、配方改变、库存调整和运行控制按风险级别审批；
- 所有写入具备对象范围、版本前置条件、幂等键和回滚记录；
- 每个 run 设置最大步骤数、工具调用数、仿真时长、候选数、令牌和费用预算；
- 工具支持超时、取消、有限重试、断点恢复和重复调用去重；
- 工厂名称、蓝图说明、上传文档和第三方资产元数据均按不可信输入处理，不能通过其中的文本改变 Agent 系统指令或权限；
- 凭据、令牌和内部提示不得写入模型上下文、事件流或审计详情；
- 仿真结果记录工厂版本、规则版本、种子、时间窗和环境，保证可复现；
- 所有 Agent 建议都必须区分观测、推导、假设和预测，并显示不足以得出结论的数据缺口。

## 51.11 Agent Control Room UI

Agent 不新增一个孤立聊天页，而是在统一控制室中提供：

- 目标输入和工作模式切换；
- 当前计划、步骤状态、取消和重新规划；
- 工具活动与证据入口；
- 诊断结论和可定位到 3D 对象的高亮；
- 原工厂与候选分支的 3D 差异叠加；
- 多方案指标表、时间序列和 Pareto 对比；
- Patch 变更清单、风险、审批、应用和回滚；
- 历史 run、已批准经验和可管理记忆。

主界面以目标、状态、结果和操作为主体，不展示长篇聊天记录，不使用解释性小字或装饰性英文眉题。复杂证据、工具参数和审计详情按需展开；所有按钮文案遵循项目现有无句末标点规则。

## 51.12 API 演进原则

现有 `/api/agent/sessions`、结构化 `AgentSuggestion` 和 Agent SSE 作为 B8 兼容基线保留。后续新增 run/plan/tool/approval/branch/patch 资源，不把所有高级状态继续塞进 Redis session：

```text
POST /api/agent/runs
GET  /api/agent/runs/{run_id}
POST /api/agent/runs/{run_id}/messages
POST /api/agent/runs/{run_id}/cancel
POST /api/agent/runs/{run_id}/replan
GET  /api/agent/runs/{run_id}/events
GET  /api/agent/runs/{run_id}/branches
POST /api/agent/approvals/{approval_id}/approve
POST /api/agent/approvals/{approval_id}/reject
POST /api/agent/patches/{patch_id}/apply
POST /api/agent/patches/{patch_id}/rollback
GET  /api/realtime/agent/{run_id}/stream
```

SSE 只推送状态变化和结果引用；大快照、Patch、指标序列和分支结果通过普通 API 分页读取，避免重连时重复传输大对象。

---

# 52. 项目 UI 结构

## 52.1 UI 美术方向

ForgeCore 整体采用：

> **工业数字孪生 + 科技控制中心**

视觉方向以项目归档的 10 张参考图片为大致美术依据，详细提炼、单图摘要、文件索引、SHA-256 与执行审计清单见：

- `docs/ui-art-style-reference.md`
- `docs/assets/ui-style-reference/`

这些图片仅作为 UI 美术风格和视觉方向的大致参考，不是高置信度的真实 UI 设计稿，也不是产品需求、页面结构或交互逻辑的事实来源。

可以严格参考：色彩倾向与明暗氛围；材质、光影、纹理和视觉层次；面板、边框、圆角、装饰和图标语言；字体气质、信息密度和整体视觉节奏；状态反馈的美术表现方式。

不得仅凭参考图片推断或照搬：信息架构和页面布局；功能入口、业务流程和数据结构；字段名称、文案和真实业务状态；交互逻辑、导航关系和权限设计；精确尺寸、断点或设备适配方案。

实际 UI 设计必须以项目需求、业务逻辑、信息层级、可访问性、响应式要求和技术约束为依据。参考图片只约束美术方向；当参考图片与真实需求冲突时，以真实需求和可用性为准，但最终视觉风格仍需尽量保持一致。

## 52.2 核心视觉语言

- 使用黑、白、浅灰和炭灰构成洁净、直接、理性的中性基底；黑色承担品牌、主操作、标题和关键数据，白色承担内容承载，浅灰承担分组与层级。
- 工作区 UI 不以黄色或渐变作为品牌主色，也不把状态色铺满页面；黄色、青蓝、绿色、橙色和红色只保留给 3D 场景、运输路线、设备状态和异常语义，并必须同时提供文字、图标、形状或位置线索。
- 3D 工厂是编辑工作区的第一视觉主体，必须居中并占据绝大部分可视区域；导航、工具、对象库、属性与状态信息以不挤压场景的悬浮窗或浮动工具条叠放在场景周围。
- 面板采用白色或浅灰材质、克制圆角、细描边和极弱投影；组件必须共用统一的圆角、边框、阴影与间距系统，并通过悬浮、展开、收起和状态过渡体现层级。
- 标题和关键数字使用明显的大字号与字重层级，正文与字段保持舒适行高；不得在 UI 中放置解释性小字、装饰性英文眉题或难以扫描的微型标签。
- 功能图标以统一笔画的黑白线性语言为主，3D 设备缩略图只作为内容资产；状态不能只依赖颜色表达。
- 视觉节奏采用“居中的高密度 3D 场景 + 周边疏朗浮窗”；默认只展示完成当前任务必需的信息，统计明细、许可路径和高级配置按需展开，禁止把大量数据平铺堆砌在主工作区。

## 52.3 UI 决策优先级

UI 设计与实现按以下优先级决策：

```text
真实产品需求与业务规则
→ 用户任务、信息层级与数据关系
→ 可访问性、响应式与技术约束
→ 本节规定的美术方向
→ 单张参考图中的局部构图或装饰
```

参考图中的三栏、左右侧栏、顶部导航、底部控制条或中央大场景只可作为视觉构图观察。页面的信息架构、导航、交互和响应式行为必须依据真实需求重新设计。ForgeCore 当前已经确认的产品原则是“场景优先、工具悬浮、信息渐进披露”；它来自编辑任务本身，也与参考图的灰白工业视觉方向一致。

## 52.4 主界面概念骨架

下列骨架来自 ForgeCore 当前产品功能关系，用于表达全画布 3D 工作区与浮动工具之间的概念分区，不是从参考图片推断的精确尺寸或断点：

```text
┌──────────────────────────────────────────────────────┐
│  ◇ ForgeCore / Factory A     已保存       ▶ 运行      │ ← 浮动顶栏
│                                                      │
│ ┌────┐                                   ┌─────────┐ │
│ │导航│                   3D FACTORY       │属性浮窗 │ │
│ │图标│                   CENTERED         │按需展开 │ │
│ └────┘                                   └─────────┘ │
│                                                      │
│ ┌── 机器 / 货物仓库 / 货架 / 传送带 / AGV / 无人机 / 运行 ┐ │
│ └──── 单击选择 · 鼠标随行 · 网格点击/拖绘建造栏 ───┘ │
└──────────────────────────────────────────────────────┘
```

工厂地块必须表现为有明确边界、尺寸和单元格反馈的实体网格，而不是无边界的装饰性参考线。设施占地、鼠标随行预览、碰撞校验和输送路径均以同一网格坐标为准；进入建造态后应在鼠标指向位置显示所选物体及其占地范围，并在越界或冲突时给出可辨识反馈。所有已放置的非传送带对象，包括机器、货物仓库、货架、缓冲区、AGV 和无人机，均可从场景中按下并拖动调整位置；轻微点击只负责选择，超过移动阈值后才进入拖动态，防止误操作。拖动过程必须持续吸附工厂网格，合法网格实时移动对象，冲突、越界或导致已连接线路与其他建筑/线路相交时保持上一合法位置并显示红色无效目标反馈。机器或仓库移动后，所有已登记的入料/出货传送带端点必须按原端口编号同步重算，不能留下断口，也不能把连接改挂到其他端口；货架移动只更新其占地和视觉，不存在输送端点。

机器、货物仓库、缓存区、AGV、无人机和传送带均从底部建造栏进入场景。建造栏只提供一个通用“机器”建造项，不把不同模型外观误当成不同业务设备；该通用机器统一加载候选 E `machine-fortified.glb`，新机器默认不绑定配方，用户在属性浮窗中自行命名，并从“配方工艺”页面创建的配方中选择绑定。通用机器在 1m 基准网格上采用 `6m × 6m` 偶数占地，机器中心必须落在整数网格交点；可见外壳放大到约 `5.4m × 5.4m`、高 4.8m，并在四周保留约 0.3m 的占地余量。未旋转时本地 `-X/+X` 两侧分别布置编号 1–3 的三个入料口和三个出货口，三条轴线分别偏离机器中心线 `-1/0/+1m`；传送带起点只吸附空闲出货口、终点只吸附空闲入料口，一个端口成功连接后不再参与吸附。机器在手上时用滚轮按 90° 步进旋转，落地后通过文档级 `Q/E` 快捷键或属性按钮旋转；六个端口和已有连接均按编号随机器更新。旧存档中的较小通用机器在恢复时统一迁移为 `6m × 6m`、把中心吸附到当前工厂网格、把视觉引用迁移为候选 E，并为旧连接分配不重复的 1–3 号端口；超过三条的同侧旧连接解除机器端关系，等待用户重新布线。

货物仓库使用候选 F `machine-window.glb` 和 Kenney 纸箱模型组成，采用相同的 6×6 占地、三通道和六端口网格协议。仓库属性浮窗直接列出所有已定义物品、当前数量、手动入库控件和逐物品无限供应开关，同时展示三个入货口和三个出货口的连接状态；每个出货口行内独立提供 `0.25–60` 秒出货间隔，并可分别选择该出货带运输的物品，不再设置仓库级统一节拍。没有物品时提供前往物品定义页的引导。场景仓库上方标签同步显示有限库存总量、无限供应项数和前两项物品名称，使用户不打开物流页也能识别仓库内容。纸箱、黑帘、内部带和库存标签属于场景表现，Inventory 才是数量事实来源。用户单击任一建造项后进入持续建造态，所选物体随鼠标在网格上吸附预览；在任意有效网格位置单击即可放置，并可继续放置同类物体。再次单击当前建造项、按 `Esc` 或单击鼠标右键均立即取消建造；单击另一建造项则直接切换。建造栏卡片本身不是需要拖入场景的对象，各卡片使用统一的黑白灰容器底色，当前项使用黑色反相表达，传送带卡片不得使用独立的常驻色块。

货架在建造栏中与货物仓库并列，采用 mastjie 开放架体、8×2 占地、约为修复前异常拉伸长度一半的架体、四层双排五列共 40 个满载箱体和独立的 `shelf` 业务类型。箱体底面必须按四层层板顶面标高校准，横向覆盖架体而不是挤在两端或悬在架外。其属性浮窗显示“无限堆叠”、实际有限数量和逐物品无限设置，不显示出货间隔、入货口或出货口；有限数量不受总容量上限限制，物流页的容量列显示 `∞`。场景陈列箱体不得替代 Inventory，仓库的 1800 件有限容量规则也不得套用到货架。

底部浮动操作栏采用具有明确职责的多标签结构，不设置含义模糊的“快捷”杂项页。“建造”标签承载建筑和传送带工具，并保留持续建造与取消规则；切换离开该标签时必须退出当前建造态，避免用户在调节其他参数时误放建筑。“运行”标签直接提供启动/暂停、`1×/2×/5×/10×` 倍速、重置、仿真时间、逻辑步数和在途数量，仍保留“空白项目没有有效物流或生产链时禁止启动”的业务门禁。“视图”标签只调整本地场景表现，包括地块网格、端口/库存/对象标签、高质量阴影和当前对象属性面板，不修改工厂业务数据、模型参数或存档内容。标签使用 ARIA `tablist/tab/tabpanel` 语义，当前态使用黑色反相和明确图标表达，并在窄屏下重新排布而不挤压中央 3D 画面。

传送带既是可铺设的实体设施，也是连接上下游设备的业务关系。用户单击传送带建造项后，在地块网格上按住指针开始铺设，沿网格拖动并在释放时完成。系统把用户实际经过的格点序列整理为连续水平/垂直段：同方向格点合并为直线，只有轨迹方向真实改变时才自动识别拐点并使用弯道模型；不得为纯直线路径凭空补拐弯，也不得把自由拖绘退化为只看起终点的固定 L 形算法。弯道按模型真实开放边确定朝向，并占用拐点中心的完整转向模块；渲染层以弯道接口为直线段端点，统一裁切两侧直线，禁止直线穿心、弯头反向、模型重叠、断口和极短直线模型缩放畸变。路径可包含零个、一个或多个拐点，可在未连接设备时独立存在；若起点或终点接近机器或货物仓库端口，则自动登记 `fromObjectId/fromPortIndex` 与 `toObjectId/toPortIndex`，并使用位于黑帘之后的对应机内锚点绘制首尾段，不能只连接到占地外轮廓。场景预览与 Store 写入均执行统一的建筑/传送带占用检测，阻止建筑重叠、传送带共格或交叉、建筑覆盖传送带和传送带穿越无关建筑。仿真中的物料必须按各段实际长度沿整条折线路径推进，不能穿过拐角直线插值；多输出机器按每条已连接出货带配置的 `outputItemId` 分流，单输出配方自动映射唯一产物；货物仓库在 TransitItem 抵达帘后锚点时入库，有限库存从出货口发车前扣减并保持跨设施物料守恒，显式开启的无限供应则作为可审计仿真源创建货物且不扣实际数量。

运输的业务结算继续使用 `0.25s` 固定仿真步长，以保证不同帧率下的库存、到货和加工结果一致；显示层不得直接把离散的 `TransitItem.progress` 当作唯一画面坐标。传送带黄色流动标记、机器/仓库内部带面标记和在途物品均应在相邻逻辑状态之间按渲染帧连续推进，并以各段实际长度计算位置，在拐点处沿折线进入下一段且平滑转向，不得表现为逐格跳动或斜切拐角。视觉推进速度必须同步 1×/2×/5×/10×仿真倍速，暂停时立即冻结，重置、恢复或时间回退时重新对齐逻辑状态；插值仅负责表现，严禁提前修改库存、完成到货或绕过固定步长业务判定。

运行时性能采用“固定业务步长、低频状态发布、逐帧视觉插值”的分层契约。应用以 `200ms` 为界面发布周期，在一次隔离的运行时草稿内完成其中全部 `0.25s` 固定步；工厂尺寸、楼层、配方、物品 schema、输送路径等静态设计数据保持引用稳定，只复制会发生变化的仿真、库存和设施状态，禁止按每个固定步深拷贝整份项目。Zustand 页面只订阅所需字段，顶层壳体不得因车辆位置或在途进度变化而整页重绘。AGV 二维 A* 与无人机三维 A* 使用确定性最小堆维护开放集，并按工厂尺寸、楼层与静态设施布局缓存二维阻塞格和三维阻塞体素；动态载具仍实时写入临时障碍。阻挡对象改变时立即重规划，持续动态阻挡按 `8` tick 节流，无静态可达路线时按 `20` tick 重试，避免同一失败搜索每秒重复执行且不牺牲及时绕行。

场景运行时自动关闭实时投影并把渲染像素比压低到约 `0.75–1.15`，暂停后恢复用户“高质量阴影”设置和约 `1–1.5` 像素比；详细 HTML 标签仅为当前选中且可交互的设施显示。机器/仓库内部流动标记、每条传送带的流动标记以及 AGV/无人机路线箭头和中间节点使用 `InstancedMesh` 批量绘制；车辆路线只删除严格同向共线的冗余节点，长路线对未选中车辆均匀稀疏为最多 `12` 个箭头和 `32` 个中间节点，选中车辆提高到 `48/96`，终点、转折、半透明状态色和由车辆向目标外流的方向语义保持不变。本轮性能调整不得改变逐层开关、“仅当前层 / 多层透视 / 多层实显”、上下楼层对称显示或当前层唯一建造网格规则，也不引入新的多层 LOD 行为。

## 52.5 UI 评审门禁

每个页面和重要组件在设计评审与实现验收时，必须使用 `docs/ui-art-style-reference.md` 第 6 节的清单，至少确认：美术氛围一致；面板、边框、层级、装饰和图标语言一致；未照搬参考图的页面结构或业务信息；已按真实需求重做信息架构与交互；满足可访问性、响应式和可读性；所有风格偏离均记录原因；参考图片始终只被描述为美术风格参考。

## 52.6 账户入口、主菜单与空白项目

应用启动后不得直接进入或自动恢复演示工厂。未建立会话时先显示登录/注册页面；登录成功后进入主菜单，由用户明确选择“新建空白工厂”或“继续已有工厂”。注册、登录和会话数据必须经独立 repository 管理。当前实现优先调用 FastAPI 的 JWT 注册/登录/刷新/登出接口，access/refresh token 暂存于浏览器 localStorage 以完成 MVP；后端使用 bcrypt 保存口令摘要，refresh token 撤销状态存于 Redis。后端不可用时才回退到原有账户隔离的浏览器副本。正式部署仍应把 refresh token 迁移到 HttpOnly Secure cookie，并补充 HTTPS、速率限制、审计日志和更严格的密钥管理。

账户入口采用 ForgeCore 黑白极简全屏 Hero：固定透明导航、真实背景视频、底部白色渐变内容区和 Inter 字体；登录/注册仍保留完整可访问表单，但不在入口展示功能说明清单或过小装饰文字。移动端视频保持约 80% 视口尺寸，桌面端铺满视口，并通过 `motion` 提供导航、视频、标题和表单区域的渐入动画。主菜单背景视频使用项目本地 `assets/media/1.mp4`，不依赖远程视频可用性；主标题固定为“选择工程”。全部 UI 可见文案、状态消息、错误提示和通知不在末尾使用句号，句中仍可按语义保留分隔。

新建工厂只能创建一块空白网格及基础仿真容器，不得预置机器、传送带、物品、配方、库存、生产事件或所谓演示存档。每个账户的远程工厂、本地回退副本和页面偏好相互隔离；只有用户主动保存后，主菜单才允许继续该账户的已有工厂。返回主菜单时暂停仿真但不隐式覆盖存档，退出登录后必须清理当前内存工作区。

---

## 52.7 全站黑白极简 UI 更新（2026-08-18）

本轮将黑白极简视觉从账户入口扩展到主菜单、固定顶栏、侧栏、七个业务页面、工厂编辑器浮层和底部工具坞，形成单一的工作区视觉契约：

- 全局字体使用 Inter，标题、关键数字和主操作提高字号与对比度；移除 `DIGITAL FACTORY`、`FACTORY WORKSPACE` 等装饰性眉题，以及页面内不参与操作的解释段落和过小辅助文案。
- 顶栏与侧栏采用黑白胶囊和几何 Logo；保存、主页、运行、导航和退出等动作使用图标按钮并保留可访问名称，当前导航使用黑色圆形反相，不再依赖黄色常驻高亮。
- 主菜单与账户入口使用全屏真实视频、白色渐变内容区和 `motion/react` 入场动效；业务表单、空白工厂设置和存档入口仍保留，不把营销说明写入界面。
- 总览、仿真、物品、配方、物流和资产页面统一白色内容面、浅灰分组、黑色主按钮、细边框和最多 8px 圆角；真实库存、许可、路径、连接、运行和错误状态继续保留，不因视觉简化而删除业务事实。
- 工厂编辑器继续以 3D 场景为第一视觉主体，楼层栈、属性浮窗、建造/运行/视图工具坞和传送带栏统一为黑白灰浮层；黄色、青蓝、绿色、橙色和红色仅在场景内表达路线、选择、运输和异常语义。
- 移动端优先保证大字号、点击区域和底部导航可见；通知 Toast 避开底部导航，桌面和移动端均不得出现文字重叠或固定顶栏遮挡主操作。
- 物品模型页使用 Vite `BASE_URL` 解析模型目录与预览资源；目录区分载入、成功和失败状态，首屏模型预览优先加载，失败时提供明确重试动作，避免目录或图片未完成时页面看起来为空。
- Toast 保留手动关闭，并按级别自动消失：成功/信息 3 秒、警告 4.5 秒、错误 6 秒；组件卸载和手动关闭都会清理对应计时器。

本轮 UI 变更只调整呈现层，不改变 FastAPI 账户接口、工厂存档、物品/配方 schema、库存守恒、输送仿真、AGV/无人机寻路或第三方资产许可边界。浏览器回归覆盖主菜单、总览、编辑器、物品、配方、仿真、物流和资产页面，并以 TypeScript 检查和生产构建作为提交门禁。

# 53. 项目核心交互流程

用户进入系统：

```text
创建工厂
```

↓

```text
创建 Item
```

↓

```text
创建 Recipe
```

↓

```text
放置 Machine
```

↓

```text
绑定 Recipe
```

↓

```text
连接 Conveyor
```

↓

```text
建立 Warehouse / Rack
```

↓

```text
配置 AGV / Drone
```

↓

```text
启动 Simulation
```

↓

```text
实时观察工厂运行
```

↓

```text
AI Analyze
```

↓

```text
Optimize
```

---

# 54. MVP 第一阶段

项目第一阶段不要试图实现所有内容。

建议 MVP 只完成：

```text
可向上堆叠的多楼层工厂

网格建造

通用机器

传送带

自定义Item

36 个首版默认参数化物品模型与模型选择器

Recipe

机器加工

3D物品运输

基础产量统计

工厂保存
```

即实现最核心的一条链路：

```text
Item A

↓

Machine A

↓

Item B

↓

Machine B

↓

Item C
```

并让整个流程可以真实运行。

MVP 的物品建模不从空白占位体起步，而是以 `assets/3d/core/items/v1/catalog.json` 为默认模型选择和参数面板的数据源。首版验收必须覆盖：36 个稳定 `modelId` 均可加载；结构参数能重新生成几何；材质参数可表现同形异材的 Item；批量 Item 可实例化渲染；`Mass` 与 `Stack Size` 仅影响业务仿真而不改变模型。

## 54.1 初版可运行实现基线（更新至 2026-08-18）

当前仓库已建立可直接运行的 Web 初版。实现入口为 `src/main.tsx`，采用 React、TypeScript、Vite、Zustand、Three.js 与 React Three Fiber；静态 3D 资产由 Vite 直接挂载仓库 `assets/`，核心物品目录运行时地址为 `/3d/core/items/v1/catalog.json`。

初版已落地以下可操作能力：

- 八个任务页面：运行总览、工厂编辑、物品模型、配方工艺、仿真监控、物流仓储、Factory Agent、资产与许可；
- 多楼层全画布 3D 工厂场景、当前层网格视图、对象选择、放置、旋转、删除、碰撞与边界校验；用户可输入层高向最高层顶部连续新建上层，并在左侧层栈切换当前编辑网格。每层右侧具有默认开启的独立显示开关，关闭层在任何材质模式下都不显示对象；楼层面板提供“仅当前层 / 多层透视 / 多层实显”三档显示模式，网格始终只属于当前层，多层模式同时显示当前层上下所有已开启楼层，并将非当前层按真实相对标高显示为统一半透明或正常材质且保持只读。跨层倾斜传送带仅在两端楼层均开启时显示。编辑工具、对象库、楼层、属性和状态采用浮动窗口，不以固定大侧栏压缩场景；
- 场景视觉以已入库的真实资产优先：机器、货物仓库、仓内纸箱与输送组件使用 Kenney Factory Kit，独立开放式货架使用 mastjie `glb/rack.glb` 并组合 40 个 Kenney 箱体形成四层双面满载陈列，AGV 使用 Cels 原件中剥离展示地板后的主体子树，无人机使用 Count Infinity 原件；只有缓存区、选择框、碰撞代理、路径、安全包络和加载失败回退等确实缺失的内容使用程序化几何；
- 登录、注册与主菜单入口；登录后由用户选择新建空白工厂或继续当前账户已主动保存的工厂，不自动创建、注入或恢复任何演示存档；
- 空白项目默认没有业务 Item、Recipe、库存、机器或传送带；建造栏只提供一个通用机器。配方工作区支持新建、编辑、启停与删除 `1–3` 种输入、`1–3` 种输出的配方，空物品库提供明确前置引导；用户随后可为机器自行命名并绑定配方；
- 通用机器、货物仓库、开放式货架、普通传送带、跨层倾斜传送带、缓存区、AGV 与无人机的网格建造，建造/运行/视图三标签底栏，以及固定步长确定性仿真、逐帧平滑运输表现、1×/2×/5×/10×速度、暂停与重置；通用机器采用候选 E `machine-fortified.glb`，货物仓库采用候选 F `machine-window.glb` 并塞入 Kenney 纸箱，两者均为 6×6 占地、三条内部带、两端黑帘和编号 1–3 的三进三出端口；仓库三个出货口分别配置 `0.25–60` 秒独立节拍与冷却，任一端口发货或等待均不冻结其他端口，旧仓库级节拍存档恢复时复制到三个端口。独立货架采用 8×2 占地和 mastjie 开放架体，不提供输送端口。AGV 采用 4×4 占地和整体 2× 后约 `3.5×2.1×2.9m` 的 Cels 主体视觉，仅允许在 1F 配置起点、终点、货物、每趟件数、一直运输或起点/终点库存阈值触发；运行时执行预约、装货、八方向 A* 安全斜向最短路、静止车辆动态绕行、固定通行权让行、卸货和真实库存结算。无人机复用同一可视化任务编程流程，但默认 30kg 小载荷并可选择任意楼层的仓库或货架；货物仓库取货使用旋转后的出货口外侧候选航点，存货使用入货口外侧候选航点，三个同侧端口由三维 A* 选择可达最短点，货架仍使用上方安全点。运行时执行真实库存预约、装卸和守恒，采用直线/面对角/体对角完整 26 邻域三维 A*，向下跨层先移至净空列再垂直下降，忽略传送带和 AGV，但避开机器、仓库、货架、缓存区三维包络及其他无人机，并以稳定载具 ID 消除互相礼让活锁。库存预约已拆成“出库现货预留”和“入库容量占位”：前者只限制可取/可发库存，后者只参与终点容量计算，物流页和仓库属性面板分别明示，旧存档从活动载具任务重建。手持建筑使用滚轮左右旋转，落地设施用文档级 `Q/E` 左右旋转。所选设施随鼠标吸附预览，再次单击同一建造项、按 `Esc` 或单击鼠标右键取消；普通传送带按用户实际拖绘轨迹铺设，直拖不产生弯道，轨迹转向时自动使用弯道模型；普通与跨层传送带的全部空闲端头都可继续吸附拉线，并与方向正确的机器/仓库端口共同形成可连续转交物品的输送链。端头不显示额外小球；续接方向改变时自动把共享端点替换为正确朝向的弯头，并裁切相邻直线/坡面。跨层段可选择向上或向下运输，沿真实楼层标高倾斜并在目标层继续连接，四向滚轮旋转时宽轴保持水平、不发生左右侧滚。建筑与传送带共享逐层网格碰撞门禁，多输出配方和多品类仓库可按出货带设置物品；仓库可直接查看全部物品、手动入库和逐物品无限供应；货架允许手动维护有限数量无限堆叠和逐物品无限状态，并由 AGV 或无人机取货、存货。有限库存运输保持物料守恒，无限供应作为显式仿真源单独审计；
- 真实产量、WIP、队列、设备利用率、输送占用、运输时间、库存与生产事件；
- 36 个核心模型的目录、分类、预览、完整参数 schema、实时三维参数编辑器及在途模型映射；未覆盖参数的 TransitItem 按稳定 `itemModelId` 加载已审计 GLB，带覆盖参数的 TransitItem 复用同一原创生成器构建真实变体，不再用白色方块代替正常物品；
- repository 以 HTTP 后端为优先路径，封装 JWT 账户、账户隔离工厂保存/恢复、页面偏好和离线回退；首次远程保存会自动创建工厂，后续使用全量快照同步；主菜单仅在当前账户存在主动保存的工厂时提供“继续”；
- 仓储库存调整、仓库三进三出自动运输、无输送端口货架的无限堆叠、1F AGV 运输仿真、无人机跨楼层三维运输仿真，以及第三方资产许可/运行时适用状态。AGV 与无人机业务运输均已就绪，但 Cels 与 Count Infinity vendor 视觉仍保持 `derived pending`；业务可用与资产派生状态不得混写。
- 运行性能基线：`0.25s` 确定性业务步长不变，以 `200ms` 批量发布运行时草稿并保持静态设计数据引用稳定；应用壳体和编辑器使用细粒度 Zustand 选择器。二维/三维 A* 使用确定性最小堆、静态障碍缓存和受阻重规划节流；运行时降低 DPR、关闭实时阴影，暂停后恢复用户画质设置。设备内部/传送带流动标记及车辆路线标记采用实例化批绘，非选中设施不常驻详细 HTML 标签，长路线仅压缩共线冗余并均匀限制标记密度。多层显示、逐层开关和楼层 LOD 契约不因本轮优化改变。

为同时满足“真实模型优先”和资产审计边界，编辑场景按需加载 AGV 与无人机 vendor 原件作为**视觉表现层**：AGV 运行时只选择主体子树并排除展示地板，再按明确目标尺寸整体放大；无人机按显式目标尺寸显示，并由独立运行时状态驱动绝对高度、朝向、载货与三维路线。AGV 的 4×4 占地、2m 导航半安全包络、3.8m 多车间距，以及无人机的 1.4m 建筑净空、3m 多机中心距、30kg 默认载荷、26 邻域三维路线和任务状态都不从 vendor 网格推断。当前 AGV 与无人机视觉均不得标记为 `derived-ready`，规模化性能、LOD、简化碰撞体和语义节点仍须在 `assets/3d/derived/` 完成审计。

## 54.2 初版运行与验收命令

Windows 本地使用者优先双击仓库根目录 `启动ForgeCore.cmd`。该脚本以自身位置作为工作目录，默认严格使用 `127.0.0.1:4173`，自动检查 Node/npm、按需安装依赖、识别已经运行的 ForgeCore 服务、拒绝误占用端口，并在服务就绪后打开浏览器。服务运行在用户自己的可见终端中，不依赖 Codex 临时任务；终端必须保持打开，按 `Ctrl+C` 或关闭窗口即停止。可通过第一个参数指定替代端口，例如 `启动ForgeCore.cmd 4175`。

```text
docker compose up -d postgres redis
cd backend
python -m pip install -e '.[dev]'
alembic upgrade head
uvicorn app.main:app --host 127.0.0.1 --port 8010

cd ..
npm install
npm run dev -- --host 127.0.0.1 --port 4173 --strictPort
npm run check
npm run validate:floors
npm run validate:agv
npm run validate:drone
npm run validate:warehouse-dispatch
npm run build
```

首轮验收基线为：TypeScript 工程检查通过；生产构建成功；确定性 1×/5× 等价性与物料守恒通过；浏览器可完成启动仿真、产生实绩、暂停、保存、刷新恢复、模型目录读取、场景对象放置与七页导航。首版允许保留性能优化项，但不得以静态演示数据替代上述真实链路。

## 54.3 后端初版交付基线（B0-B10，更新至 2026-08-19）

后端从 B0 到 B10 已形成可运行闭环：

- **B0**：FastAPI 应用、SQLAlchemy async 数据层、Alembic、Docker Compose、`/api/health`，健康检查同时验证 PostgreSQL 与 Redis；
- **B1**：bcrypt 口令摘要、JWT access/refresh、注册/登录/刷新/登出/当前用户和 owner 权限门禁；
- **B2**：工厂创建/列表/读取/删除与全量同步。楼层、对象、物品、配方、库存、连接配置和嵌套 JSON 配置按快照原子覆盖写入，并通过唯一约束和引用校验阻止重复或悬空 ID；
- **B3**：仿真状态、指标序列、活动历史的读写接口；浏览器继续负责确定性仿真计算，后端只保存和恢复状态；
- **B4**：Redis Pub/Sub 工厂事件总线和工厂 SSE；指标、活动、仿真状态、全量同步事件均有稳定事件名和 JSON 数据；
- **B5**：前端 `apiClient`、认证与工厂 repository HTTP 优先、access 过期自动刷新、远程存档恢复、网络不可用时本地回退；
- **B6/B7**：蓝图保存/读取/更新/删除、公开搜索/标签/排序、收藏、fork、fork 记录和 `.fcbp` 导入导出；
- **B8**：Redis Agent session、结构化 `AgentSuggestion`、progress/suggestion/branch result/error 事件、事件查询与 Agent SSE；LLM provider 尚未绑定，契约已稳定。
- **B9**：在保留 B8 session 兼容接口的基础上，增加 PostgreSQL `AgentRun`、`AgentStep`、`AgentToolCall`、`AgentRunEvent` 和 Alembic 迁移；交付 12 个只读工具（含工厂依赖图、目标约束解释、机器/配方链/传送带/产能等），支持 run 创建、幂等分析、取消、历史查询、owner 门禁和 SSE 进度。前端 Factory Agent Control Room 展示目标、五步计划（goal → context → metrics → diagnostics → summary）、工具活动、证据、诊断、历史运行和 3D 对象定位；无 LLM provider 时由确定性分析器真实运行，可选 OpenAI 兼容 provider 仅用于工具选择降级。
- **B10（A2 最小可验收）**：增加 `AgentPatch` / `AgentApproval` 持久化与 `plan_design` 模式；确定性规则根据诊断 findings 生成 `move_object` / `update_config` / `adjust_inventory` 操作，在内存分支上做引用、楼层、库存、占地 AABB 与硬约束（如面积、AGV 数量）校验；主工厂在审批前不变；提供 approve / reject / apply / rollback API 与 Control Room 差异预览。Apply 通过既有全量 snapshot sync 写入，并强制 `base_version` 冲突拒绝。本切片不含 headless 仿真、3D 差异叠加与增删设备。

后端验证基线为 `pytest -q`、`ruff check app tests` 和 `mypy app`；前端验证基线为 TypeScript 工程检查、楼层/AGV/无人机/仓库节拍领域回归和生产构建。2026-08-18 浏览器验收已覆盖注册、创建、全量同步、SSE、退出、重新登录和远程恢复。

---

# 55. 第二阶段

其中多楼层与跨层倾斜传送带已提前在 2026-08-15 交付；第二阶段余项为：

```text
货运升降机

Rack

Buffer

Splitter

Merger

库存系统
```

默认物品模型库的第二阶段候选按需求源扩展为：Washer、Screw、Cable、Jerry Can、Gas Cylinder、IBC、Housing、Frame、Assembly Module、Pipe Component、Powder、Liquid、Grain、Flake、Small Product、Medium Product、Large Product、Palletized Product、Capsule、L Shape、U Shape。它们尚未纳入 v1 的 36 个已交付 `modelId`；实施时应延续参数化模型原则、分配稳定 ID、升级模型库版本并补齐迁移和资产审计，最终总量控制在约 50～60 个基础模型。

此时已经形成完整的：

> **数字生产线设计与仿真平台**

---

# 56. 第三阶段

加入：

```text
AGV

A*寻路

物流任务

自动补料

自动入库

无人机

跨楼层运输
```

第三阶段只有在 AGV/无人机的 derived 运行时 GLB、尺度与朝向、碰撞体、导航安全包络、LOD、语义节点、动画来源及 CC BY 署名审计全部通过后，才能把对应视觉接入标记为完成；仅把 vendor 原件复制进仓库不等于完成运行时适配。

其中 AGV 的 1F 可视化任务、库存触发、A*、真实搬运和基础多车协调，以及无人机的跨层同构任务编程、26 邻域三维 A*、净空列安全下降、真实搬运、三维轨迹和基础多机协调，已于 2026-08-17 提前交付；仍未完成的是 Cels/Count Infinity derived 运行时视觉资产、机器装卸适配器、智能充电、长时间窗空地交通预约及正式物理碰撞。AGV 跨层运输不再列为目标，跨层物流由无人机和跨层传送带承担。

此时项目升级为：

> **生产 + 仓储 + 智能物流数字孪生平台**

---

# 57. 第四阶段

第四阶段完成 Agent A1-A2，从现有 B8 会话契约升级为可用的“有依据分析 + 受控方案设计”：

### A1 Grounded Analyst

- 接入可替换的 LLM provider，但保留无模型时的确定性分析降级；
- 建立带工厂版本的 Context Builder，不把整个工厂 JSON 无选择地塞入上下文；
- 注册工厂快照、依赖图、指标、事件、瓶颈、库存、机器、配方、传送带、AGV 和无人机只读工具；
- 实现目标编译、证据引用、数据缺口提示和因果诊断；
- 建立 Agent Control Room 的目标输入、计划步骤、实时进度、诊断结果和 3D 对象定位；
- 将 run、step、tool call 和审计从短期 Redis 会话扩展为 PostgreSQL 持久化记录。

A1 验收场景为：用户询问产能不足原因，Agent 能读取指定版本工厂与一段仿真历史，指出生产或物流根因，引用具体对象与指标；删除 LLM 生成的解释文本后，底层证据仍可通过工具独立复算。

**已落地（至 B10 前）**：12 只读工具、FactoryGoal 编译、工厂依赖图、Control Room、PG run/step/tool/event、确定性诊断；LLM 仅可选地选择工具。

### A2 Planner + Patch

- 实现可检查计划、工具预算、取消、超时、有限重试和重新规划；
- 建立 `FactoryGoal`、`FactoryPatch`、前置条件、版本冲突、风险分级、反向操作和幂等契约；
- 支持设备增删移动、配置、配方、库存策略和物流任务等结构化操作，但只写临时分支；
- 接入规则校验、差异预览、审批请求和拒绝后重规划；
- 正式应用必须由用户批准，应用后重新读取工厂并验证，支持一键回滚。

A2 验收场景为：Agent 根据“面积和 AGV 数量不变”的约束生成合法 Patch；分支内端口、碰撞、配方、库存和楼层校验全部通过；主工厂在审批前保持不变；主工厂版本变化时旧 Patch 被拒绝而不是覆盖新修改。

**最小可验收已落地（B10）**：`plan_design` 模式、确定性 Patch 生成（移动/配置/库存）、内存校验与差异摘要、审批前后主厂隔离、`base_version` 冲突拒绝、批准后 apply 与 inverse rollback。仍缺：headless 仿真分支评分、3D 差异叠加、设备增删与多方案对比、LLM 直接写 ops。

---

# 58. 第五阶段

第五阶段完成 Agent A3-A4，从单一建议升级为可批量验证的高级优化器：

### A3 Simulation Branch

- 把确定性仿真抽取为前端与 Node worker 共用的无界面 TypeScript 核心；
- 后台创建、运行、取消、保留和清理 Simulation Branch，不依赖浏览器持续在线；
- 支持固定预热期、评估期、种子和多倍速无渲染运行；
- 对比产量、WIP、缺料、堵塞、机器利用率、库存、运输距离、装卸等待和约束满足情况；
- 支持故障、断供、路径封锁和处理时间波动等可复现场景；
- 提供 3D 差异叠加、指标时间序列、风险说明和应用前复核。

A3 验收场景为：同一基线、规则版本、配置和种子在浏览器与 worker 得到相同业务结果；Agent 可以生成至少两个分支，运行相同时间窗并给出可复算对比；取消和服务重启后 run 不丢失且不会重复应用 Patch。

### A4 Multi-objective Optimizer

- 用解析容量和图算法先定位约束，减少盲目仿真数量；
- 用 CP-SAT 处理设备数量、排产、订单和任务分配；
- 用合法布局构造器和启发式搜索处理设备位置、仓储位置和物流拓扑；
- 用多目标 Pareto 搜索评估产量、成本、占地、WIP、物流和风险；
- 联合优化生产、仓储、传送带、AGV 和无人机，不把瓶颈从一个子系统转移到另一个；
- 为每个候选记录算法、种子、预算、停止原因、完整 Patch 和仿真证据。

A4 核心演示目标为：

```text
把当前工厂产能提升到 120 件/分钟
面积和 AGV 数量不变
库存峰值不高于当前方案
```

Agent 至少返回最低改造量、最高鲁棒性和最短运输三个非支配方案；若目标不可行，则报告硬约束冲突和最小放宽建议，而不是伪造达标结果。

---

# 59. 长期高级功能

长期阶段完成 Agent A5-A6，在明确策略和审计边界下扩展持续自治与跨工厂协同。

### A5 Operations Agent

- 订阅实时指标和活动，持续识别缺料、堵塞、停机、拥塞和交期风险；
- 在用户定义的动作白名单、对象范围、风险、费用和时间预算内执行策略托管；
- 支持订单优先级、动态排产、补料、物流重派、充电和装卸点时间窗协调；
- 支持设备故障、预测维护、能源峰谷、质量损失和碳排目标；
- 超出策略授权的动作自动暂停并请求审批，任何正式设计变化仍保留 Patch 和回滚。

A5 验收不能只看平均指标，必须覆盖长时间仿真、异常注入、权限越界、重复事件、断线恢复和最坏场景；Agent 既要证明完成目标，也要证明没有突破用户预算和安全约束。

### A6 Enterprise Agent

- 多工厂产能分配、订单分流、供应商时延和供应中断推演；
- 多人审批、角色权限、评论、方案交接和审计导出；
- 工厂蓝图社区的语义检索、许可过滤、适配评估和受控 fork；
- 组织级知识库、指标定义、工艺规范和可管理 Agent 记忆；
- 私有模型接入、数据驻留、租户隔离、费用配额和合规保留策略。

### 暂不优先

以下内容不作为 A1-A3 前置项：自行训练大模型、无审批全自动改厂、开放任意代码执行、仅为聊天检索引入独立向量数据库、没有确定性基线的“多 Agent 辩论”。它们不能提升当前闭环的可信度，且会提前放大成本和安全面。

最终形成由用户设定目标和边界、Agent 负责规划与验证、确定性引擎负责事实、审批与 Patch 系统负责控制的完整工业仿真与优化平台。

---

# 60. 项目创新点

## 60.1 自由数字工厂设计

系统不是固定展示某个工厂，而允许用户从零设计生产环境。

---

## 60.2 通用工业物品系统

用户可以自行创建任意原料、半成品和成品，但不必为每一个物品重复建模。ForgeCore 以“稳定基础模型 + 参数化变体 + Item 业务数据”为核心：首版 36 个 first-party/core 模型覆盖基础形状、原材料、机械零件、电子部件和包装容器，同一 `modelId` 可通过尺寸、截面、孔径、齿数、圈数、材质和颜色等参数表示大量物品。

官方模型不足时，用户仍可上传自己的 GLB；但物品重量、堆叠数、配方身份和库存状态始终与模型参数分离。这使模型网格能在大量 Item 间复用和实例化，同时保留用户对具体业务物品的自由定义能力。

---

## 60.3 通用生产工艺系统

通过：

```text
Item

+

Recipe

+

Machine
```

建立通用工业生产逻辑。

系统不会绑定特定行业。

---

## 60.4 实时 3D 生产仿真

物品会真正沿着生产线运输，并通过机器发生变化。

---

## 60.5 多层立体工厂

用户可以建立多楼层工业设施，通过：

```text
同层传送带与可切换上下行方向的跨层倾斜传送带

升降机

无人机
```

实现跨楼层生产物流。

---

## 60.6 智能物流

AGV可以自动接受运输任务并使用路径规划算法寻找最佳路线。

---

## 60.7 生产与物流统一仿真

系统不仅模拟机器加工，同时模拟：

```text
仓储

库存

运输

物流拥堵

生产堵塞
```

---

## 60.8 Agent 生产优化

Agent 不只是聊天入口，而是根据工厂真实仿真数据定位生产瓶颈、生成结构化 Patch、在分支中验证并申请执行。

---

## 60.9 Agent 方案推演

Agent 提出优化方案后先在模拟副本中运行，再比较优化效果并申请用户审批。

---

## 60.10 高度模块化

整个项目可以不断扩展：

```text
新设备

新物流方式

新生产规则

新算法

新AI能力
```

不会受到最初工业场景限制。

---

# 61. 项目开发难度控制

项目整体看起来功能庞大，但可以采用分阶段方式降低实现难度。

核心系统实际上建立在几个成熟技术之上：

```text
3D场景
→ Three.js

网格建造
→ 坐标系统

生产模拟
→ 状态机

传送带
→ 路径插值

AGV
→ A*算法

实时数据
→ SSE，协作编辑再引入 WebSocket

数据可视化
→ ECharts

Agent 编排
→ FastAPI + LLM + Tool Registry

产能分析
→ 规则与统计算法
```

项目不需要：

```text
自行训练大型AI

开发真实PLC

连接真实工业设备

开发专业工业物理引擎

完成高精度流体或机械模拟
```

因此能够保持：

> **高视觉表现力 + 高创新性 + 高扩展性 + 可控开发难度**

---

# 62. 项目演示场景

最终答辩可以设计一个完整流程。

## STEP 1

进入系统。

展示一个空白三层工厂。

---

## STEP 2

创建三种物品：

```text
Metal Plate

Motor

Machine Frame
```

---

## STEP 3

创建：

```text
Electric Motor
```

配方。

---

## STEP 4

在工厂中放置：

```text
Machine A

Machine B

Machine C
```

---

## STEP 5

使用传送带连接。

```text
Storage
↓
Machine A
↓
Machine B
↓
Machine C
↓
Warehouse
```

---

## STEP 6

启动仿真。

物品开始真实沿生产线运输。

---

## STEP 7

加入第二条生产线。

制造：

```text
Motor
```

---

## STEP 8

将：

```text
Motor

+

Machine Frame
```

送入装配机器。

生产：

```text
Final Product
```

---

## STEP 9

建立二楼仓库。

加入：

```text
AGV

Drone
```

进行物流运输。

---

## STEP 10

运行一段时间后显示：

```text
Production：
68/min

Target：
100/min
```

---

## STEP 11

询问 AI：

```text
为什么产量不足？
```

---

## STEP 12

AI分析：

```text
Machine B利用率：

99%

其余机器：

约60%

Machine B为主要生产瓶颈。
```

同时：

```text
AGV平均等待时间较高。
```

---

## STEP 13

Agent 提出：

```text
增加Machine B

增加Buffer

调整物流路线
```

---

## STEP 14

系统建立仿真副本并测试 AI 方案。

---

## STEP 15

显示结果：

```text
Current：
68/min

Optimized：
94/min

+38.2%
```

整个流程能够完整体现：

```text
3D

数字孪生

工业生产

物流

算法

AI

仿真

优化
```

---

# 63. 项目最终愿景

ForgeCore 最终希望构建一个：

> **用户可以像搭积木一样设计工厂，又能够像真实工业软件一样分析和优化工厂的 AI 数字孪生平台。**

用户不需要针对某个具体行业重新开发系统。

只需要定义：

```text
生产什么？

需要什么原材料？

经过哪些加工？

如何运输？
```

ForgeCore 就能够建立对应的数字生产系统。

最终形成：

```text
用户需求

↓

自由设计工厂

↓

定义生产工艺

↓

建立物流系统

↓

数字仿真

↓

数据采集

↓

Agent 诊断

↓

分支优化

↓

方案推演

↓

用户审批
```

形成完整的智能工厂设计与优化闭环。

---

# 64. 项目关键词

```text
Digital Twin

Smart Factory

Factory Simulation

Artificial Intelligence

Industrial AI

Production Simulation

Factory Design

AGV

Autonomous Logistics

3D Visualization

Path Planning

Production Optimization

Manufacturing

Industry 4.0
```

---

# 65. 项目一句话介绍

> **ForgeCore 是一套基于 Web 3D 数字孪生与人工智能的通用智能工厂设计、生产仿真和优化平台，用户可以自由定义物品和生产工艺、搭建多层生产线、配置自动仓储及 AGV/无人机物流系统，并通过 AI 对整个工厂进行生产瓶颈分析和产能优化。**

---

# 66. 项目核心宣传语

> **Design. Build. Simulate. Analyze. Optimize.**

中文可以表述为：

> **设计工厂，搭建产线，模拟生产，洞察瓶颈，智能优化。**

---

# 67. 项目核心价值总结

ForgeCore 与普通数字孪生系统最大的区别在于：

传统数字孪生主要解决：

> **“如何把现实中的工厂显示出来？”**

而 ForgeCore 希望进一步解决：

> **“如何自由设计一个工厂？”**

> **“这个工厂能否正常运行？”**

> **“它能生产多少产品？”**

> **“生产瓶颈在哪里？”**

> **“物流系统是否合理？”**

> **“怎样调整才能生产得更快？”**

并最终让 AI 帮助用户回答：

> **“怎样设计一个更好的工厂？”**

因此项目最终不是单纯的：

> 3D 工厂展示系统

而是逐渐发展成为：

# AI-Powered Digital Factory Design, Simulation & Optimization Platform

即：

# AI 驱动的数字工厂设计、仿真与优化平台
