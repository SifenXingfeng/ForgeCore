# ForgeMind 项目方案

> 状态：融合版唯一权威方案  
> 当前基线：2026-08-20  
> 维护目录：本文件所在的 ForgeMind 项目根目录

## 1. 文档权威与融合原则

本文件是融合版 ForgeMind 后续产品范围、当前能力、系统架构、数据边界、资产规则、启动方式和路线图的唯一权威方案。根目录 `README.md` 面向使用者，`docs/ForgeMind-当前实现总览.md` 记录代码事实，其他设计文档提供专题细节；发生冲突时，以本文件与实际代码为准，并同步修正文档。

本项目由组员的 ForgeMind 版本与原 ForgeCore 能力融合而来。融合遵循以下原则：

1. 保留 ForgeMind 已有的登录、三维编辑、确定性仿真、后端、视觉检测、生成式工厂和语音/助手设计。
2. 继承 ForgeCore 已落地的 36 个参数化物品、AGV/无人机模型、八方向 AGV 导航、资产许可约束、UI 参考审计和确定性模型生成方法。
3. 历史文档不直接冒充当前实现；原始资料归档在 `docs/archive/forgecore/`，迁移状态见 `docs/ForgeCore-融合迁移审计.md`。
4. 从本基线开始只修改当前 ForgeMind 项目目录。旧 ForgeCore 目录仅作为已经归档过的来源，不再作为维护入口。

## 2. 项目定位

ForgeMind 是面向智能工厂设计、物流规划、生产仿真、三维展示和方案评估的 Web 数字孪生平台。它允许用户在网格场地中配置设备、物品、配方、传送带、仓储和运输车辆，通过确定性仿真观察物料流、背压、产出与设备状态，并使用规则规划器或可选远程 AI 服务辅助提取约束和解释方案。

项目当前以可离线启动、可复现仿真和人工审核为底线。大语言模型不是仿真、寻路、碰撞、物料守恒或布局验证的真相源，也不是运行项目的必需依赖。

## 3. 当前技术架构

| 层 | 位置 | 当前职责 | 是否为默认启动依赖 |
| --- | --- | --- | --- |
| React 前端 | `src/` | 页面、编辑、三维场景、状态管理和浏览器内仿真 | 是 |
| 确定性仿真 | `src/game/simulation.ts` | 固定步长、物料流、机器、AGV 和无人机运行态 | 是 |
| 宝钗渲染层 | `src/engine/daiyu/` | 批处理、嵌入模型、渲染预算和场景表现 | 是 |
| Spring Boot | `backend/` | 用户、工厂结构和私有导入资源持久化 | 推荐入口启动；仅前端模式可省略 |
| MySQL | `docker-compose.yml`、`.forgemind/mysql-data/` | Spring Boot 的结构化持久化 | 推荐入口启动；仅前端模式可省略 |
| FastAPI 辅助服务 | `ai-service/` | 规则助手、可选 DeepSeek、ASR/TTS 和视觉检测网关 | 否，可选 |

前端使用 React 18、TypeScript、Vite、Three.js/React Three Fiber、Zustand 和 Anime.js。Spring Boot 3/Java 17 与 MySQL 8.4 只保存可恢复结构，不逐 tick 接管浏览器仿真。FastAPI 不进入仿真主循环。

## 4. 当前已实现能力

### 4.1 工厂编辑与数据

- 空白工厂项目、动态多楼层、网格放置、旋转、占地和碰撞校验。相邻楼层标高差统一为 `5.25m`；当前最高的内置可建造机器经视觉比例校准后约高 `4.75m`，上一层楼板底面约位于 `5.17m`，两者保留约 `0.42m` 净空。
- 建造视角的右键平移限制在水平地面，不改变镜头与目标点高度；建造工具激活时，右键用于取消当前放置。
- 当前编辑层的已放置建筑支持键盘变换：单选建筑后，`W/A/S/D` 分别沿网格上/左/下/右移动一格，`Q/E` 分别左旋/右旋 `90°`，`Delete` 删除；移动和旋转必须继续经过建造边界、占地和同层/跨层碰撞校验，失败时不得写入半完成状态。按住 `Shift` 并拖动左键显示屏幕空间框选矩形，一次选择当前编辑层内与框相交的多个建筑；多选只提供批量选中与一次性 `Delete` 删除，不得让只读上下文楼层进入选择集合。批量删除记为单次可撤销操作，单选移动、左右旋转和删除同样进入现有撤销/重做历史。键盘快捷键由工作台级捕获监听统一接管，并在事件发生时读取当前选择，不依赖 3D 场景的焦点、挂载时机或 `Suspense` 生命周期；活动画布必须可由程序聚焦，并在左键点选或 `Shift` 框选开始时主动取得键盘焦点，避免焦点残留在侧栏输入控件导致快捷键被误判为表单输入。只要当前工厂存在有效选择，`Delete` 删除的优先级不得再受视图、面板开关或项目弹窗状态影响，并兼容标准 `Delete`、数字键盘删除及系统报告为退格的删除键。框选控制器同样必须挂载在 3D 资产的 `Suspense` 加载边界之外，从活动画布上的文档捕获阶段开始，在文档级持续跟踪移动与释放，并优先于镜头控制接管本次指针操作。该链路必须有浏览器级输入验收，覆盖输入框持有焦点后真实 `Shift + 左键拖动` 抢回画布焦点、生成框选结果，以及随后按下 `Delete` 清空所选建筑。
- 传送带采用按住左键直接描线：指针快速移动时自动补齐网格，改变方向时自动生成正交转点，沿原路回拖可撤销末端，无需右键逐点锁定转角。
- 建造目录提供向上、向下两种跨层倾斜传送带，使用当前项目的 `roller_conveyor_segment.glb` 重复拼接为真实坡面。坡度固定为 `75%`，相邻层高差 `5.25m` 对应水平投影 `7m`、坡面长度 `8.75m`；从 L1/L2 放置并连接上一层，旋转后支持四个水平方向，同时校验上下两层投影占地，避免建筑穿模。坡道的上下端暴露与普通传送带一致的输入/输出接口；放置坡道或拖绘普通传送带接近端点时按兼容方向自动吸附，避免留下错一格或反向的断链。
- 楼层采用 ForgeCore 原有的“当前编辑层 + 每层独立上下文显隐”契约，并支持在工作台内手动向上追加楼层。点击楼层只切换当前编辑标高，不改动该层显示开关；当前层显示优先级高于开关，因此即使开关关闭也会立即显示、可选择和可建造，切走后再按原开关状态决定是否作为只读上下文保留。场景只复用原有的一块 `GridFloor` 毛玻璃地面及其 1m 网格，并随当前编辑标高移动，不额外叠加第二套网格或楼板。其他已开启楼层仅显示建筑和物流对象，不显示地面、毛玻璃或网格，并且保持只读、不可选择、不可拖动和不可从详情面板修改。跨层传送带在两端楼层按“当前层优先或开关开启”的有效可见性显示。
- 新建空白工厂不预置业务物品、生产配方或默认流水线；物品与工艺路线均由用户显式新建。底部“物品详情”是物品库的独立维护入口，物品以用户指定的稳定 ID 为业务主键，支持名称、分类、说明、颜色、质量、堆叠上限、36 个 ForgeCore 基础模型及其全部参数化覆盖值的新增、编辑和删除；物品维护不得嵌入“机械制造”。该页面沿用旧版“物品与模型库”的目录优先结构：已添加物品和可视化模型库占据主界面，提供分类、搜索、缩略预览、模型详情与大预览；新建和编辑只能通过按钮进入悬浮编辑窗，不允许常驻表单挤压物品目录。编辑窗左侧必须显示可旋转缩放的实时 3D 模型，右侧按“结构与几何 / 材质与表现”完整展示模型目录定义的参数，参数变化须即时反映到预览。
- 底部“生产路线”是生产配方和工艺路线的唯一维护入口，支持用户指定 ID、说明、启停、加工时长、多输入/多输出以及新增、编辑和删除，不得退化为只读展示页，也不得在“机械制造”内复制第二套工艺编辑入口。页面必须保留既有的“配方目录 / 配方详情 / 当前工艺链”三栏总览与视觉结构；新增、编辑使用聚焦当前工艺的悬浮编辑窗，不能在悬浮层中再次塞入目录并以通用左右分栏表单替换总览页面。
- 底部“机械制造”只维护基础加工机器定义，并引用“生产路线”中已经存在的工艺。机器定义必须包含稳定机器 ID、名称、模型来源（现有设备模型或用户导入模型）、占地、运行参数、入口数量、出口数量和已录入工艺路线；入口/出口数量不得超过对应占地边能够容纳的接口数，录入路线的输入/输出物品种类数不得超过该机器的入口/出口数量。机器定义可新增、编辑和删除，并在保存时持久化。页面与物品详情采用一致的目录优先结构：已添加机器和内置/导入模型库占据主界面，模型可视选择并提供独立大尺寸实时 3D 预览；新建、编辑进入悬浮窗，调节模型、占地、高度、吞吐、功率和接口等参数时须能查看实时效果，不允许将新建表单常驻在仓库列表旁。当机械制造工作区已经展开且用户继续选择场景建筑时，设备详情悬浮窗必须切换为位于工作区之上的短窗模式，以较小最大高度和内部滚动保留完整关闭、配方与操作入口，不得继续以普通整高面板被下栏或工作区遮挡。
- “机械制造”中建立的机器自动进入建造面板“基础加工”；场景总览只能从该机器已经录入的工艺路线中选择，不能把未登记路线临时绑定给实例。删除仍被场景实例引用的机器定义必须被阻止，避免产生悬空设备。建造面板展开时，任何已选建筑的设备详情都必须使用短窗模式并在自身内部滚动；该规则不依赖机械制造工作区是否打开，避免详情窗被建造面板遮挡。
- “精密装配”是固定能力设备而非可改装的基础加工机器：仅接受至少两种输入且仅一种输出的多合一装配路线；实例可在总览中调节入口/出口数量并选择所有满足端口容量的已启用路线。其入口可沿后三边布置，最大数量等于后三边长度之和；出口沿前边布置，最大数量等于前边长度。
- 蓝色入口与黄色出口是实际物流拓扑接口而非装饰。每个可见标志所在的网格就是可吸附、可收发货且参与仿真的真实接口格；机器定义或精密装配实例改变端口数量时，同步改变标志、吸附候选和仿真可连接接口。拖绘传送带靠近空闲兼容接口时直接吸附到可见标志所在格，物料只能经已连接的真实入口进入、经真实出口离开；多出口按稳定轮询顺序分配。所有场景接口标识必须位于应用视觉缩放后的模型包络之外，并以同色连接桥指向设备本体；不得只把图形外移却继续用模型边缘的隐藏旧格计算仿真。旧存档中已经接到模型边缘端口的线路可作为迁移兼容继续收发货，但新建、预览、吸附和连接状态统一以可见外置标志为准。
- 楼层拥有可编辑名称并随存档持久化；楼层切换、独立显示开关和“当前层高优先级显示但不改开关”的既有契约保持不变。
- 登录后先进入“新建工厂 / 加载工厂”入口，不再直接注入默认流水线。新建工厂和导入 JSON 都只建立当前会话中的未保存草稿，不得因此自动创建或覆盖正式后端存档；只有用户明确点击“手动保存存档”后，才新建或覆盖当前账号下的正式项目，用户未手动保存便退出时不得把草稿写入正式存档。系统另维护每个账号唯一的一份“自动恢复存档”，在加载列表最上方单独标识；定时恢复写入只能覆盖这一固定槽位，不能生成历史堆积，也不能改写任何正式存档。加载自动恢复存档后仍视为未保存草稿，下一次手动保存必须新建正式项目。加载列表允许用户确认后删除任意正式存档或清除自动恢复槽；删除当前打开的正式存档只解除其保存目标，不清空内存中的工厂。版本化 JSON 文件仅作为明确标注的“导入 JSON / 导出 JSON”辅助能力，不得替代后端主存档流程。后端同时继续负责登录和用户私有 GLB 资源。
- `.forgemind-project.json`/兼容 JSON 与 GLB 资源包导入、预览归一化、封面生成和用户隔离。

### 4.1.1 货物仓储与传送物流

- 建造分类统一为“货物仓储”和“传送物流”。“货物仓储”包含“货物存取站”“货物仓储架”“入货仓库”和“出货仓库”；仓储架合并原原料仓储架和成品缓存仓的能力并使用原料仓储架模型，成品缓存仓不再出现在传送物流分类。
- 货物存取站采用 `4×4` 占地：前侧只有一个朝向传送带的物流口，后、左、右三面各有一个居中的 `2×2` 货物仓储架泊位。放置仓储架靠近空泊位时自动吸附到完整泊位，反向先放货架再放存取站时同样可吸附；旋转站体后泊位随朝向旋转。吸附必须形成可查询的实际“站体侧 ↔ 货架对象 ID”拓扑，而不是仅靠距离或动画猜测。
- 取货模式下，机械臂只能从目标侧实际吸附的货架库存取得指定物品，再放到前侧出货传送带；存货模式下，只能从前侧入口传送带取货并写入目标侧实际吸附货架的独立库存。显式物品映射指向未连接侧、取货货架缺货、目标货架达到容量上限或前侧传送带无法接收时必须阻塞并保留来货，不允许凭空产出、吞货或写入站体虚拟库存。没有显式映射时，第一次存入某种物品会在当前实际连接且有剩余容量的货架中按固定种子确定性选侧，后续同类继续合并；自动取货只在实际连接且有该物品库存的货架中选择。普通货物仓储架保存按物品拆分的初始库存和有限总容量，仿真期间每次真实存取都更新对应货架数量，不提供无限供货。存取模式、物品映射和 `0.25–60s` 存取间隔可配置并保存；仓储工作区按货架对象显示运行时真实库存、已用/上限容量与连接状态。
- 入货仓库是显式无限供货边界：用户选择其供应物品后，可由输出传送带、AGV 或无人机无限取得；每次实际取得的数量计入对应物品“消耗”，仓库自身不扣减库存。出货仓库是显式无限容量终点：可接收输入传送带、AGV 或无人机送达的任意物品，入库后不可再次取出，每次实际接收的数量计入对应物品“产出”。普通仓储架、入货仓库和出货仓库的语义不得通过一个含糊的“无限”开关互换。全局产出/消耗账本只统计这两个工厂边界：机器加工、普通货架存取和货物存取站搬运不得重复计数。
- 普通货物仓储架、入货仓库和出货仓库都允许为每个场景实例设置独立显示名称。名称属于对象存档字段，设备详情、仓储总览以及 AGV/无人机的起点和终点选择必须优先显示该名称，并保留设备类型、楼层或短 ID 作为辨识信息；旧存档没有名称时继续显示原设备类型，兼容旧对象中的 `name`/`customName` 字段迁入，不得因缺少新字段拒绝加载。
- 存取站内嵌短传送带复用普通直行传送带的滚筒模型、深灰输送面和低密度浅灰运动条纹，输送面世界高度与地面传送带一致。内嵌短带的位置必须由前侧实际蓝/黄接口的局部中线决定：站体处于 `0°/90°/180°/270°` 任一朝向、或在取货/存货模式间切换时，短带始终与当前接口位于同一前侧、同一条中心线。短带外端和吸附后的外部单格传送带内端必须在格边界无缝对接，不得出现换向后落到另一条中线、半段叠压、横向错位或重复带面。暂停时条纹保留但相位冻结；取货模式运行时沿站体前向向外滚动，存货（入货）模式运行时从当前相位平滑反向并朝站内滚动，不得跳相位或仍向外运动。机械臂动画按实际后/左/右泊位与前侧输送面往返，取货是“货架 → 带面”，存货使用同一路径反向执行，并在夹取至放置全过程显示随夹具移动的货物。
- 精密装配从其后三边已连接的入口取得原料，完成加工动画后由前边出口送出；多个出口按端口编号稳定轮询，暂停时传送与取放动画一并冻结。

### 4.2 仿真与渲染

- 固定步长和种子化随机数保证同一输入可复现。
- 传送带采用离散槽位和头堵背压；未启动或暂停仿真时，输送面仍保留低密度浅灰运行条纹作为静态方向提示，但滚筒和条纹均不得移动，也不显示方向箭头。启动仿真后，浅灰色长方形横向条纹从当前相位沿物料方向连续、匀速、平滑移动；直线、左右平面拐角和跨层坡道共用同一视觉速度与间距，拐角条纹须沿 `90°` 中心圆弧连续转向，不得以跳帧、折线或黄色箭头替代。物料跨段时保留同一货物 ID，由渲染层连续阻尼插值，避免跨格重建造成跳变；未连接到明确下游的传送带末端必须把货物保留在 `offset=1` 并形成背压，不得用“离开工厂”语义静默删除货物。物料、机器、产出和消耗仍以仿真快照为准。
- 跨层倾斜传送带进入同一确定性物料拓扑：向上线路按低层到高层输送，向下线路按高层到低层输送，运输时间按真实 `8.75m` 坡长计算，端点通过自动吸附接口续接普通传送带并继承背压语义；坡面模型、接口标记和在途货物沿实际三维路径渲染，不产生侧滚。
- Three.js 只负责呈现，高频仿真状态不由 React 组件各自推导。
- 宝钗渲染层负责静态模型、传送带和机器人批处理，以及 DPR、阴影和运行预算。
- 除 AGV 与无人机保持 `1×` 外，全部可建造建筑在既有比例上统一追加 `1.25×` 渲染放大；仓储、来料站、原料架、分流/汇流等普通建筑为 `1.25×`，原先已校准为 `2×` 的生产机器累计为 `2.5×`。通用机器与 AGV 的视觉宽度比由约 `1.41:1` 进一步提升至约 `1.76:1`。
- 传送带保持每个网格单元约 `1.05m` 的纵向长度，避免连续线路错位，只将高度和横向床宽从原 `2×` 累计放大至 `2.5×`，视觉床宽约 `1.0m`；直线、弯道、建造预览、选择框和在途货物高度使用同一视觉基准。平面拐角不得使用与直行段无关的低矮转盘或方底盘拼接，必须采用与直行段一致的深灰输送面、浅色铝型材框架、护栏、支腿和蓝色传感器，输送面中心线以单格内 `90°` 圆弧连续连接入口与出口，端部床高、带宽和护轨位置与相邻直行段对齐。该比例校准不改变占地、端口、寻路、物料数量或确定性仿真结果。

### 4.3 物流车辆

- AGV 是独立运输实体，不是带入货口/出货口的建筑物，场景中不显示机器蓝黄接口；直接选中车体后必须能查看状态和编辑运输任务。AGV 支持起点、终点、物品、每趟数量、中间站、优先级、运行策略以及“一直运输 / 起点与终点库存条件触发”。选中 AGV 后，详情悬浮窗必须沿用旧版三节点“起点装货 → 物品/每趟件数 → 终点卸货”编程结构并显示实时状态；供货方式、起点阈值、终点阈值、策略和优先级必须在窄悬浮窗中按独立行纵向排布，不得压缩到同一行造成文字竖排或控件只剩狭缝。新放置或未编程 AGV 必须空载待命，禁止自动注入演示路线、默认物品或虚拟载荷。普通货物仓储架可直接作为 AGV 起终点，不要求旁边存在货物存取站；装货时只检查并真实扣减所选货架内对应物品，货架缺货或数量不足时阻塞等待，不得凭空供货。选择入货仓库作为起点时才允许无限取得并登记消耗；卸到普通货架时真实占用其有限容量，送入出货仓库时登记产出。
- AGV 使用八方向确定性 A*，横纵/斜向代价为 `1/√2`，斜向禁止切角；设施和动态车辆进入安全包络，运行时支持让行、重规划和恢复路径。
- 无人机与 AGV 一样是没有机器入/出货接口的独立运输实体，并共用可保存的可视化运输任务与“一直运输 / 库存条件触发”契约；可配置任意楼层的仓储/货架起点、货物、每趟数量和终点。选中无人机后同样直接在详情悬浮窗中使用三节点编程并显示楼层与三维路径状态。无人机不再被限制在 L1 停机位或固定升降井；新放置或未编程无人机必须空载待命，普通货架可被无人机直接取放且只以该货架真实库存和容量为准，不依赖货物存取站；真实装卸遵守与 AGV 相同的有限货架、无限入货边界和不可逆出货边界。无人机实例必须提供与运行时位置同步的稳定选中命中体，不能只依赖第三方 GLB 的实例化三角面射线；当前活动层的可见无人机点击机体任意主要部位都应打开载具详情，只读上下文层继续保持不可选择。
- 无人机使用确定性 26 邻域三维 A*，横纵、面对角和体对角代价分别为 `1/√2/√3`，同时改变水平与垂直坐标完成任意方向跨层飞行；设施体积、建筑净空和其他无人机进入三维安全包络，路线和实体位置使用绝对楼层标高渲染。无人机已经位于起点或终点的安全悬停网格时，A* 返回的单节点零距离路径必须直接按“已到站”处理并执行真实装卸，不得因为路径长度不足两个节点而永久停在等待规划状态。

### 4.4 诊断与生成式工厂

- 黛玉思考引擎通过规则解析、Recipe Graph、设备估算、候选搜索、端口路由、碰撞校验、副本仿真和经济性计算生成可比较方案。
- 新生成布局必须使用入货仓库作为无限原料边界、出货仓库作为产量边界，不得再用无真实库存来源的货物存取站或普通货架伪造无限供货；生成方案的副本仿真与主场景共用同一套边界产出/消耗账本。
- A-01 有现有产线时优先生成最小调整、重布线和重构候选；A-02 支持从需求生成新布局。
- 模型服务只能提取受限 `GenerationSpec` 或解释结果；坐标、设备数量、路线、碰撞和指标必须由确定性算法和仿真确认。

### 4.5 视觉检测与助手

- `inspection.html` 提供独立视觉检测工作台和隔离路由演示。
- 智能管家动作使用 `contracts/forgemind-assistant-tools.json` 的 `1.0.0` 协议，并由服务端/前端双重校验。
- 默认无 AI 服务时使用浏览器内规则降级，不阻塞建造、仿真和生成式工厂。

## 5. AI 与语音部署政策

### 5.1 默认模式

项目默认只启动前端，不安装、不启动也不探测 Ollama、Qwen 或其他本地大语言模型。规则解析器、仿真、AGV 导航、无人机三维寻路、诊断和候选布局均可独立工作。

### 5.2 可选服务

- 需要工具协议时使用启动参数显式开启 `ai-service`；视觉、ASR 或本地 TTS 还需另行安装可选完整依赖，不进入默认或轻量 AI 门禁。
- `ai-service` 默认 `FORGEMIND_LLM_PROVIDER=rule`，不访问任何大语言模型。
- 如需远程约束提取或助手回答，可显式配置 `FORGEMIND_LLM_PROVIDER=deepseek`、`DEEPSEEK_API_KEY` 和可选模型名。
- API Key 只存在于服务端环境变量，不进入浏览器代码、存档或仓库。
- ASR/TTS 属于可选语音能力，不得成为前端启动门禁。

## 6. 资产与模型体系

### 6.1 当前目录

- `public/models/forgecore/items/`：36 个 ForgeCore 原创默认物品、预览、目录和校验清单。
- `public/models/forgecore/forgecore_agv.glb`：Cels Industrial 3D AGV Trolley，CC BY 4.0。
- `public/models/forgecore/forgecore_drone.glb`：Count Infinity Futuristic Delivery Drone，CC BY 4.0。
- `public/models/industrial/`：当前工业设备拆分资产；来源与再分发边界见资产审计。
- `public/models/panda/` 与其他机器人 GLB：机器人和工作站展示资产。
- 用户导入资产：运行时 Blob URL 与后端私有资源，不写入公共模型目录。
- 机器和物品采用单一外观解析契约：编辑器大预览、目录缩略图、建造面板、场景实例以及传送带在途货物必须从同一个模型 ID / 资源 ID、模型路径和参数覆盖值解析外观。内置机器定义必须把所选内置模型的实际资产路径注册到场景定义；带自定义 `resourceId` 的机器实例不得进入按通用 `machine` 类型合批的静态模型路径。物品参数化模型的几何、材质、颜色和纹理覆盖必须同时作用于缩略预览与在途实例，静态默认 PNG 只能作为未修改默认外观的性能优化或加载失败回退，不能遮蔽用户已设定的外观。

### 6.2 ForgeCore 物品制作契约

36 个默认物品的稳定 ID、参数 schema、材质和拓扑定义由 `tools/item-models/` 中的零依赖 Node.js 生成器维护。修改几何、参数、材质或生成逻辑时必须：

1. 修改生成器，不手工改生成的 GLB、PNG、`catalog.json` 或 `SHA256SUMS`。
2. 执行 `node tools/item-models/build.mjs` 重建到 `public/models/forgecore/items/`。
3. 执行 `node tools/item-models/validate.mjs` 与 `npm run models:validate:strict`。
4. 更新本方案、`docs/资产与许可审计.md` 和 `AGENTS.md` 协作审计。

运行时契约为自包含 GLB 2.0、米制、Y-up、底面中心原点。`mass`、`stackSize` 等属于 Item 业务数据，不是几何参数。

### 6.3 许可与来源

任何第三方资产都必须保留作者、原始链接、许可证、修改声明和校验信息。无明确许可或仅允许演示的高精度资产不得被宣称为 ForgeMind 原创，也不得在未复核授权前用于公开商业分发。完整事实见 `docs/资产与许可审计.md`。

## 7. 数据与安全边界

- `src/game/simulation.ts` 是运行态真相源；数据库不逐帧保存物料或车辆坐标。
- 前端保存/加载必须经过 schema 校验和旧版本迁移。当前本地存档 schema 为 v6：在 v5 楼层名称、丰富目录、基础机器定义、实例端口配置和货物存取站程序基础上，为普通仓储架新增有限容量与按物品拆分的初始库存，并新增入货仓库/出货仓库对象类型；继续保留每个对象的楼层字段、车辆运输程序、跨层倾斜传送带参数以及可选仓储实例显示名称。v1–v5 存档迁移时生成缺失的默认楼层名称和仓储配置，v1–v6 中缺少显示名称的对象均合法并回退设备类型；旧对象继续使用内置设备定义，旧存档已有物品与配方予以保留，新建空白工厂则从零开始且车辆不带任务或货物。后端启动产物必须与源码共同接受 v1–v6 完整项目载荷，交付前不能只执行 `mvn test` 而保留未重新打包的旧 JAR。
- 后端以账号和工厂项目 ID 隔离存档，一名用户可拥有多份工厂。后端主存档必须原样覆盖完整的当前 schema，不得只保存旧版对象、物品和配方子集；项目列表保存名称、创建时间、更新时间、schema 版本以及楼层/对象/物品/配方数量摘要。旧版单工厂结构允许只读兼容迁移，但新的保存操作统一写入完整项目载荷。
- 私有导入资源由 `owner_user_id` 隔离，保存工厂时校验 `resourceId` 归属。
- 助手动作只允许协议白名单；重置、改配方和改来料绑定必须确认。
- 生成式方案必须通过边界、碰撞、端口连接和副本仿真后才能应用。

## 8. 启动与运行

Windows 推荐双击根目录 `启动ForgeMind.cmd`。该入口默认启动前端、Spring Boot 和 MySQL，以保证注册、登录和远端存档可用；无 Docker 时自动使用本机 MySQL 8.x 建立项目专用实例。`start-forgemind.bat` 不带参数时仍可作为仅前端入口。两种模式都不会启动 Ollama 或本地语言模型。

可选参数由 `scripts/start-forgemind.ps1` 维护：

- `-NoBrowser`：不自动打开浏览器。
- `-Port 5173`：指定前端端口。
- `-IncludeAI`：额外启动规则/远程 AI 网关，不启动本地 LLM；首次只安装 `requirements-core.txt` 的轻量 Web 依赖。
- `-IncludeSpring`：启动 Spring Boot，并优先使用 Docker MySQL；Docker 不可用时回退到本机 MySQL 8.x 项目实例。
- `-IncludeVoiceChat`：显式启动独立语音控制台。

## 9. 验证门禁

当前前端改动至少执行：

```powershell
npm run build
npm run sim:smoke
npm run sim:regression
npm run sim:backpressure
npm run interaction:regression
npm run visual-scale:regression
npm run manufacturing:regression
npm run multifloor-conveyor:regression
npm run floor-visibility:regression
npm run drone-navigation:regression
npm run storage:regression
npm run assistant:protocol
npm run save:regression
npm run models:validate:strict
```

生成式工厂改动另执行 `npm run generative:regression`；后端改动执行 `mvn test`；AI 服务改动至少执行 Python 语法编译和规则模式接口检查。2026-08-21 当前验证中，构建、仿真、协议、存档和模型校验均通过；生成式回归仍在 A-02 端口路由搜索阶段失败，本次具体为 `coil_1 → assembly_1` 无法找到无碰撞连接，因此不得声明候选 3/3 通过。已知失败必须在交付说明和当前实现总览中记录，不能通过修改文档伪装为通过。

## 10. 文档体系

- `ForgeMind 项目方案.md`：唯一权威方案。
- `README.md`：安装、启动和用户入口。
- `docs/README.md`：文档状态与阅读顺序。
- `docs/ForgeMind-当前实现总览.md`：代码事实索引。
- `docs/资产与许可审计.md`：当前资产、来源、许可与校验。
- `docs/ForgeCore-融合迁移审计.md`：新旧能力映射和未迁入项。
- `docs/archive/forgecore/`：逐字节保存的历史基线，不作为当前需求入口。

## 11. 当前缺口与后续路线

1. 对 `public/models/industrial/`、Panda 和其他机器人资产补齐可再分发许可文件与逐文件 SHA-256。
2. 修复 `generative:regression` 中 A-02 多输入装配端口路由失败以及 A-01 调整候选不足三个的已知边界。
3. 在已统一的 AGV/无人机真实库存装卸基础上，继续补充多车/多机共享服务位预约与更长时间窗的拥堵回归。
4. 继续让后端保持可选，逐步增加资源对象存储、分享权限和大文件治理，但不把网络服务变成离线演示门禁。
