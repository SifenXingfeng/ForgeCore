# ForgeMind 原始 Git LFS 指针归档

本目录保存组员融合版本中曾位于 `public/models/forgecore/items/` 根目录的 6 个 Git LFS 指针。它们不是可加载的 GLB 文件，也没有被当前运行时引用；对应的完整模型位于 `public/models/forgecore/items/basic/`。

为避免指针被误当作运行时资产，并让 ForgeCore 参数化物品库的 `SHA256SUMS` 完整性校验保持严格，这些原始文本按原文件名迁入本归档。
