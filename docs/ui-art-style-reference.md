# ForgeCore UI 美术风格参考与执行规范

- 状态：项目 UI 美术方向的支持性规范与参考资产审计记录
- 权威项目方案：[ForgeCore 项目方案](<../ForgeCore 项目方案.md>)
- 参考资产目录：`assets/ui-style-reference/`（相对于本文件）
- 最近更新：2026-08-18

## 1. 范围与权威性声明

这些图片仅作为 UI 美术风格和视觉方向的大致参考，不是高置信度的真实 UI 设计稿，也不是产品需求、页面结构或交互逻辑的事实来源。

可以严格参考的内容包括：

- 色彩倾向与明暗氛围；
- 材质、光影、纹理和视觉层次；
- 面板、边框、圆角、装饰和图标语言；
- 字体气质、信息密度和整体视觉节奏；
- 状态反馈的美术表现方式。

不得仅凭参考图片推断或照搬：

- 信息架构和页面布局；
- 功能入口、业务流程和数据结构；
- 字段名称、文案和真实业务状态；
- 交互逻辑、导航关系和权限设计；
- 精确尺寸、断点或设备适配方案。

实际 UI 设计必须以项目需求、业务逻辑、信息层级、可访问性、响应式要求和技术约束为依据。以上参考图片只约束美术方向；当参考图片与真实需求冲突时，以真实需求和可用性为准，但最终视觉风格仍需尽量保持一致。

本文件用于细化 `ForgeCore 项目方案.md` 第 52 节，不构成平行项目方案；若两者出现冲突，以项目方案为准，并同步修订本文件。

## 2. 核心视觉语言

| 维度 | 提炼结论 | 执行边界 |
| --- | --- | --- |
| 整体气质 | 洁净、理性、精密的工业控制台外壳，与高细节的工厂数字孪生内容并置 | 保留工业感与工程秩序，不把视觉复杂度等同于业务复杂度 |
| 色彩 | 黑、白、浅灰和炭灰构成工作区中性基底；黑色承担品牌、主操作和当前导航；黄色、青蓝、绿色、橙色和红色只在 3D 场景中承担稳定状态语义 | 参考图不提供精确色值；页面 UI 不以黄色或渐变作为主色，实施时需建立语义色令牌并通过对比度验证，不得只用颜色传达状态 |
| 明暗与对比 | 大面积明亮内容区与局部深色侧栏、标题条或悬浮控制形成强对比；3D 场景或地图可偏暗，以彩色路径和标记聚焦 | 深色区应服务层级或操作聚焦，避免无意义的大面积黑底；文字与叠层必须保持可读 |
| 材质与氛围 | UI 表面以洁净浅色、细描边、轻阴影、轻微渐变或雾化为主；3D 内容强调金属、管网、设备、网格、磨损和俯视光影 | 界面材质保持克制；工业纹理主要留在内容和场景，不扩散为影响阅读的背景噪声 |
| 面板与层级 | 白色或浅灰卡片、局部炭黑面板、小到中等圆角、细边框、弱投影；深色标题条、色条、下划线和反相填充用于分层或选中 | 建立少量稳定的容器层级，统一圆角、描边和阴影语法，避免每个模块自创样式 |
| 字体气质 | 无衬线、粗壮、直接；大标题、关键数字和编号具有明显字重层级；正文紧凑但不拥挤 | 字体名称、字号和比例须另行设计；不能照搬参考图中难辨的小字、超大展示编号或疑似生成错误文案 |
| 图标与装饰 | 以黑白线性几何图标为主，必要处使用实心反相；工业设备可使用微缩 3D 缩略图；场景中的状态线、节点、路线和轻微发光负责聚焦 | 统一线宽、尺寸与视觉重量；线性功能图标和 3D 内容缩略图应分工明确，避免无规则混用 |
| 状态反馈 | 页面使用黑白反相和布局层级；3D 场景可使用黄、青蓝、绿、橙、红等状态色，并配合进度条、轮廓、标记点、区域覆盖和路径表达 | 状态语义需由真实产品定义；颜色之外必须有文字、图标、形状或位置等冗余线索 |
| 视觉节奏 | 常见构图是高密度中央场景配较疏朗外围面板，或暗色可视化配明亮数据面板；卡片内部留白规整，组间边界清晰 | 这只是视觉密度与层次参考，不是固定三栏布局要求；页面结构必须重新依据任务流设计 |

## 3. 可执行设计规则

### 3.1 颜色与表面

1. 先建立中性背景、主表面、次表面、深色控制表面、主文字、次文字、品牌强调、信息、成功、警示和危险等语义令牌，再确定实际色值。
2. 页面 UI 以黑白反相作为品牌与聚焦强调，不使用黄色常驻高亮；黄色等状态色只在 3D 内容中按业务语义出现。
3. 青蓝、绿色、橙色和红色只在有明确语义时出现；同一语义在不同页面保持一致。
4. 默认表面使用细描边和弱投影建立层级；只有悬浮控制、关键对话或场景叠层可以提高阴影或明暗差。
5. 3D 场景、地图和图表背景必须为文字叠层预留安静区域，必要时使用遮罩、局部降噪或独立信息板。

### 3.2 组件语言

1. 卡片、面板、输入、按钮、标签和弹层共用有限的圆角层级，不为单个页面创建特殊圆角。
2. 主操作采用高对比实心按钮；次操作采用浅色或描边按钮；危险操作使用独立危险语义并避免与普通操作混淆。
3. 当前态使用黑色反相、图标和文字共同表达；一个导航层级只使用一种主高亮语法。
4. 线性图标统一笔画、端点、包围盒和视觉尺寸。设备缩略图属于内容资产，不替代功能图标。
5. 进度、路径、告警和选区必须同时提供非颜色线索，且在缩放、弱视和色觉差异场景下仍可辨认。

### 3.3 字体与信息密度

1. 标题和关键数字可以粗重醒目，正文、字段标签和辅助信息必须优先保证长时间阅读的清晰度。
2. 使用稳定的标题、分组标题、正文、辅助文本和数据层级；表格数值应对齐，单位与状态标签应清楚关联。
3. 不复用参考图中的示例文案、编号、字段、数值或疑似错字；所有内容来自产品词汇表和真实数据模型。
4. 高密度场景旁的信息面板应更克制，避免 3D 细节、彩色路径、粗标题和大量状态色同时争夺注意力。

### 3.4 布局、响应式与可访问性

1. 先依据用户任务、信息优先级和数据关系完成信息架构，再选择单栏、分栏、抽屉、浮层或全屏画布等形式。
2. 参考图中的三栏、左右侧栏、顶部导航、底部控制条和中央大场景均为构图观察，不是固定页面模板。
3. 响应式方案必须针对实际内容定义折叠、重排、隐藏与替代交互，不从图片反推精确尺寸或断点。
4. 实施和验收时检查键盘操作、焦点可见性、语义结构、文字缩放、对比度、色觉冗余、触控可用性及动态效果降级。

## 4. 单图观察摘要

以下内容只记录各图可观察的美术侧重点，不确认其中的功能、文案或交互为真实产品设计。

| 文件名 | 可观察的美术侧重点 | 不应照搬的内容 |
| --- | --- | --- |
| `01_工业基地总览_高精重制版.png` | 明亮黑白灰外壳、暖黄管线、俯视工业基地、外围轻量卡片与粗体数据 | 高密度场景细节、语义不清的顶栏符号、占高较大的入口卡 |
| `02_工厂建造模式_高精重制版.png` | 深浅侧栏夹中央网格场景、黄色模式强调、绿色选区、深色浮动工具条 | 设备名称、小字、参数关系以及图标所暗示的建造流程 |
| `03_生产连接模式_高精重制版.png` | 偏暗工业场景、青黄发光连线、黑白侧栏对照、醒目的编号与数值 | 杂乱线路、示例名称和编号、未经验证的源点/终点操作逻辑 |
| `04_物资与生产协议管理_高精重制版.png` | 明亮三段式卡片、深色栏头、黄色短线与竖条、微缩工业物件图标 | 重复规格图标、示例字段与步骤，以及装饰卡片承载全部信息的方式 |
| `05_蓝图管理_高精重制版.png` | 横向大面板、黄橙局部聚焦、工厂预览与角落深色数据浮层 | 展示编号、重复按钮、菱形符号、日期与操作关系 |
| `06_工厂仿真运行模式_高精重制版.png` | 深色指标区、中央俯视场景、浅色状态区、半透明深色运行控制 | 过密场景、小尺寸注释、空缺字段以及线性/立体图标混用 |
| `07_智能物流控制中心_高精重制版.png` | 暗地图与亮数据面板对照、高饱和路线、深色表头、状态进度条 | 坐标与列名、路线业务语义、错位标注及不平衡留白 |
| `08_AI工厂诊断_高精重制版.png` | 黑白工业基底、红黄告警、俯视厂区、徽标、虚线和区域覆盖 | 诊断逻辑、示例数值、杂乱连线与遮挡设备的覆盖层 |
| `09_AI优化方案对比_高精重制版.png` | 明亮大面板、三段式对比、深色主按钮、绿红增减反馈 | 展示编号、固定数值、装饰图例及方案比较业务规则 |
| `10_系统设置与数据中心_高精重制版.png` | 深浅分区、黄色单点强调、粗标题与大数字、规整卡片栅格 | 示例数据、无文字图标组、设置导航与权限关系 |

## 5. 参考图片索引与文件审计

- 来源根目录：`D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/`
- 项目归档目录：`docs/assets/ui-style-reference/`
- 归档方式：原文件逐字节复制，不重新编码、不改名、不覆盖来源文件；项目文件未嵌入 Markdown。
- 分析状态：10 张已分析，0 张待分析。

| 文件名与项目相对链接 | 来源路径 | 字节数 | SHA-256 |
| --- | --- | ---: | --- |
| [01_工业基地总览_高精重制版.png](<assets/ui-style-reference/01_工业基地总览_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/01_工业基地总览_高精重制版.png` | 2173159 | `b16ca4ec010a26609bc3b77a2bf15baae88b9abb158339b4834bc933692dd902` |
| [02_工厂建造模式_高精重制版.png](<assets/ui-style-reference/02_工厂建造模式_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/02_工厂建造模式_高精重制版.png` | 2060490 | `a73ff88c32960bab0ed86bd3f2db6f903c0ac6eaeda3d88101245eaaa9cdd90c` |
| [03_生产连接模式_高精重制版.png](<assets/ui-style-reference/03_生产连接模式_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/03_生产连接模式_高精重制版.png` | 2442842 | `be22228da25aed9bf0ae9766c97191d9b6e02dd796578c6b0cfc47a4a08edbbf` |
| [04_物资与生产协议管理_高精重制版.png](<assets/ui-style-reference/04_物资与生产协议管理_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/04_物资与生产协议管理_高精重制版.png` | 1344941 | `570acb5b999d908255f51e758a9cf924360440d64f580d047eef898603d1aa60` |
| [05_蓝图管理_高精重制版.png](<assets/ui-style-reference/05_蓝图管理_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/05_蓝图管理_高精重制版.png` | 1695346 | `5f3be241457ed2b89632698fa75df133f8cbc9458f5a46d664d210b446b2f473` |
| [06_工厂仿真运行模式_高精重制版.png](<assets/ui-style-reference/06_工厂仿真运行模式_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/06_工厂仿真运行模式_高精重制版.png` | 2340161 | `59bc6a2a684bc1adc6304efb2b48aed0fdf8110daf6e983855056a6c16ce9925` |
| [07_智能物流控制中心_高精重制版.png](<assets/ui-style-reference/07_智能物流控制中心_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/07_智能物流控制中心_高精重制版.png` | 1540687 | `a8460e9399ef4f878fa8226fa6c11af61c93ad5524f05c6f330cbfe221619f96` |
| [08_AI工厂诊断_高精重制版.png](<assets/ui-style-reference/08_AI工厂诊断_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/08_AI工厂诊断_高精重制版.png` | 2043218 | `c3637dc66e93e22259df9fd18324f31a537c4ebbc74be2a3390df4010bb1326b` |
| [09_AI优化方案对比_高精重制版.png](<assets/ui-style-reference/09_AI优化方案对比_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/09_AI优化方案对比_高精重制版.png` | 1434545 | `41a2461cb1a127c56fa470ea761e40c96c0f705f516fb661ea67f336710b59dd` |
| [10_系统设置与数据中心_高精重制版.png](<assets/ui-style-reference/10_系统设置与数据中心_高精重制版.png>) | `D:/Desktop/工厂系统UI_十张高精重制版_01-10(1)/10_系统设置与数据中心_高精重制版.png` | 997586 | `4df2c86dc79473c37d4edb9daebc9059078603af5fc30b823856aecc2e27e451` |

## 6. UI 审计清单

每个 UI 页面、组件集合或重要视觉变更在设计评审与实现验收时均需逐项检查。未通过项不得静默放行；确需偏离时必须记录原因、影响与批准人。

- [ ] 色彩倾向、材质表现、光影层次和整体工业科技氛围与本规范一致。
- [ ] 面板、容器、边框、圆角、阴影、层级、装饰和图标语言保持一致。
- [ ] 黑白 UI 层级稳定；黄色及其他状态色只在 3D 内容中遵循稳定语义，没有在页面间任意换义。
- [ ] 状态反馈除颜色外还有文字、图标、形状或位置等可辨识线索。
- [ ] 没有把参考图中的页面结构、三栏构图、导航关系或业务信息误当成产品事实照搬。
- [ ] 已根据真实需求、用户任务、数据关系和业务优先级重新完成信息架构与交互设计。
- [ ] 字段、文案、状态、功能入口、权限和流程均可追溯到项目需求或数据模型，而不是来自参考图片。
- [ ] 已检查可访问性、响应式、可读性、键盘操作、焦点状态、文字缩放、对比度和色觉冗余。
- [ ] 高密度 3D 场景、地图或图表没有遮挡关键文字，叠层信息在真实数据下仍清晰。
- [ ] 固定顶栏、通知、浮窗和工具坞没有遮挡或截获其他可见控件的点击区域；视觉上可见的主操作在实际命中测试中仍能触发自身。
- [ ] 所有偏离参考美术风格的地方均记录了原因、影响范围和替代方案。
- [ ] 评审材料只把这些图片描述为“美术风格参考”，没有称为正式设计稿、需求稿或交互原型。
- [ ] 参考图相对链接可访问，归档文件数量正确，文件 SHA-256 与本索引一致。

### 6.1 偏离记录模板

| 页面/组件 | 偏离项 | 原因与约束 | 对视觉一致性的影响 | 补偿措施 | 负责人/日期 |
| --- | --- | --- | --- | --- | --- |
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |

### 6.2 场景优先版实现审计（2026-08-14）

| 检查项 | 初版结果 | 实现证据/说明 |
| --- | --- | --- |
| 色彩、材质与工业氛围 | 通过 | `src/styles.css` 以雾灰/白/浅灰作为页面与浮窗基底，以炭灰表达文字和运行按钮，以暖黄表达导航当前态、连接模式、选择轮廓与输送方向；3D 地面也改为明亮工业灰，并保留金属模型、网格、阴影与黄色引导线 |
| 面板、边框、层级、装饰与图标 | 通过 | 固定大侧栏已替换为 52px 浮动图标轨；顶栏、属性检查器和底部工具坞均为半透明灰白浮窗。新增登录/注册与主菜单继续采用雾灰网格背景、灰白磨砂容器、细描边、柔和投影和 Lucide 线性图标，黄色只承担品牌、选中和主操作高亮 |
| 是否误抄参考图结构/业务信息 | 通过 | 登录、主菜单、七页工作区和空白项目流程均来自 ForgeCore 真实任务；新项目不再注入示范 Item、Recipe、机器、传送带、库存或状态，也不引用参考图字段和流程 |
| 是否按真实需求重做交互 | 通过 | 启动流程为“登录/注册 → 主菜单 → 新建空白工厂/继续已有存档”；存档按本地账户隔离。物品创建器按所选模型 schema 展示全部结构、拓扑和材质参数，包含范围/步长/单位、条件启停、实时三维预览及恢复默认；参数变体会真实重建网格或修改 PBR 材质。设备栏提供通用“机器”和“货物仓库”：机器绑定 `1–3` 种输入、`1–3` 种输出的自定义配方；候选 F 仓库直接列出所有已定义物品、数量、手动入库和逐项无限供应状态，并在出货口 1–3 的端口行内分别设置出货间隔，不再使用仓库级统一节拍。两者均采用 6×6 偶数网格占地和编号 1–3 的三进三出端口；所有普通/跨层传送带的空闲端头和方向正确的设施端口均可吸附并继续拉线，一端只允许一条连接，多输出机器或多品类仓库可按出货带指定物品。场景不再绘制端点小球，吸附状态由文字和线路高亮表达；续接方向变化时共享端点自动换成 Kenney 弯头，并同步裁切相邻直线或坡面。在途货物按 Item 的稳定 `itemModelId` 加载 36 项核心模型，带参数覆盖时显示真实生成变体，不用白色方块代替。建筑在手上时滚轮按 90° 左右旋转，端口及现有连接随朝向更新。所有已放置建筑及其他非传送带对象支持按住拖动并持续吸附网格；合法位置实时更新，冲突、越界或导致连接线路穿模的位置显示红色目标反馈并拒绝提交，机器/仓库原端口连接随移动同步重算。左侧层栈支持输入层高并向上新建楼层，每层右侧提供默认开启的独立显示开关；关闭层在任何材质模式下都隐藏对象。楼层显示设置为“仅当前层 / 多层透视 / 多层实显”，多层模式对当前层上方和下方所有已开启楼层一视同仁，非当前层按真实相对标高以半透明或正常材质显示并保持只读，三档始终只显示当前层网格。跨层倾斜传送带只有两端楼层均开启时才显示，并继续提供“向上/向下”建造方向和属性反转，其四向坡面使用稳定水平宽轴，滚轮旋转不产生左右侧滚。底部浮窗按真实职责分为“建造 / 运行 / 视图”，不增加含义模糊的快捷杂项：运行标签直接调节启动、暂停、四档倍速和重置，视图标签控制网格、场景标签、阴影与属性面板，离开建造标签会安全退出建造态。单击建造项进入持续建造态，实际物体与占地反馈随鼠标吸附网格；再次单击、`Esc` 或鼠标右键立即取消。传送带从网格按住拖绘，只有实际转向才插入弯道；建筑与传送带共用逐层占用门禁，禁止重叠、交叉和穿模。传送带流动标记、机内带面和在途物品使用逐帧位置插值，沿真实三维路径连续前进并在拐角或坡面平滑转向；暂停、倍速和重置与逻辑仿真同步，不再把 `0.25s` 固定结算步长直接表现为逐格跳动 |
| 可访问性、响应式和可读性 | 初版通过，持续复核 | 登录、注册和新建工厂表单使用原生 label、自动完成语义、错误 `role=alert` 与明确禁用状态；主菜单及工作区有语义标题和按钮。浮窗和工具坞具备 `aria-pressed` 建造状态、键盘焦点、`Esc` 与右键退出说明；选中机器提供文档级 `Q/E` 快捷键和等价可点击按钮，并跳过表单、可编辑区域与输入法组合态。浏览器真实按键复核已确认焦点位于属性按钮时 `E:270°→0°`、`Q:0°→270°` 均生效。普通任务页新增 88px 顶部安全区，浏览器命中测试确认“创建物品”和“新建配方”不再被固定顶栏的运行按钮截获；920px/640px 入口页断点及 900px/620px 工作区断点已落地。网格落点和路径拖绘仍以指针操作为首版主流程，完整键盘网格导航列为后续增强项 |
| 高密度场景叠层 | 通过 | 浏览器测得 1280×720 视口下 Canvas 保持完整 1280×720；顶部浮栏、52px 导航轨、楼层栈、属性浮窗和底部建造栏均为叠放层，不占用页面布局列。场景摘要紧凑显示当前楼层、地块、设施、传送带和在途物料 5 项，空白项目业务对象初始值均为 0 |
| 偏离记录 | 已记录 | Cels AGV 与 Count Infinity 无人机 vendor 原件按用户要求进入编辑场景：AGV 选择主体子树、排除展示地板并按三轴 2 倍目标尺寸显示；AGV 的八方向 A*、斜向防切角、安全包络、动态车辆绕行、载荷、库存预约和任务状态由 ForgeCore 独立业务数据实现，不从网格推断，vendor 外观仍保持 derived pending。无人机仅做显式尺度适配，飞行、碰撞、载荷、电池和任务语义仍待派生。 |
| 参考图身份 | 通过 | UI 与产品文案始终将 10 张图片描述为美术风格参考，未称正式设计稿、需求稿或交互原型 |

本轮未重新打开参考图片，只依据第 2–3 节已归档的文字规范检查。页面继续使用已审计的 Kenney 机器/仓库/纸箱/传送带/弯道、mastjie 货架、Cels AGV 和 Count Infinity 无人机 GLB。通用机器采用候选 E `machine-fortified.glb`，货物仓库采用候选 F `machine-window.glb`，运行时组合没有创建替代第三方网格。浏览器实测完成空白工厂建造：仓库卡显示“6×6 网格 · 3进3出”，滚轮使手持仓库从 0° 精确变为 90°，落地场景显示“入货口 1–3”“出货口 1–3”和“库存 0 件 · 0 类”；创建“纸箱货物”后物流页自动出现 1800 容量记录，加入 3 件后场景标签和属性浮窗均同步为 3 件。页面无运行错误。新项目仍不注入示范业务数据，纸箱只是货物容器视觉，实际数量来自库存记录。

多楼层专项复核（2026-08-15）：浏览器从空白工厂连续创建 2F 与 3F，标高分别为 `4.5m` 和 `9.0m`，切换楼层后网格与普通对象按当前层隔离显示。1F 放置向上倾斜段后，2F 能看到伸入该层的自由端并吸附续拉普通传送带；普通传送带的末端再次被识别为可续拉节点。2F 另行放置向下运输的 2F↔3F 倾斜段，方向切换状态正确。跨层段以 `4.5m` 高差对应 `6m` 水平投影的 `75%` 固定坡度为基准；浏览器另以 `9m` 层高复核水平投影自动增长为 `12m`，预览、放置实体、属性数据与两层端点一致，没有只抬高 Y 轴。主动保存、刷新并继续后，三层、两条跨层段和普通传送带链均恢复，楼层对象计数把跨层段计入其上下两层，页面错误日志为空。

传送带端头与坡面姿态复核（2026-08-15）：运行场景和跨层建造预览均已移除端点球。浏览器分别从跨层传送带上层端头和普通传送带自由端以垂直方向续拉，连接点均显示方向正确的弯道资产，弯点两侧没有直线穿心或重复叠放。跨层传送带在 0° 与滚轮旋转后的 90° 实体均保持轨道宽轴水平；四向坐标基断言进一步确认本地宽轴的世界 Y 分量始终为 `0`。

楼层显示模式专项复核（2026-08-15）：浏览器在 1F、2F 各放置一台机器并停留于 2F，依次切换三档设置。`仅当前层` 时只保留 2F 机器；`下层透视` 时 1F 机器按 `-4.5m` 相对标高显示，并统一以 `0.24` 透明度作为只读上下文；`下层实显` 时两层机器均以正常材质显示，当前层网格承载面仍保持单面半透明。进入下层模式时镜头会自动纳入可见高度范围，切回单层时恢复原单层距离；三档均只绘制 2F 当前网格，下层没有重复网格。网格与半透明承载面关闭深度写入，跨层传送带及其在途物品以更高透明绘制优先级穿过当前网格面连续显示，但仍被当前层不透明机器正确遮挡；页面错误日志为空。

逐层可见开关专项复核（2026-08-18）：楼层列表改为“楼层选择按钮 + 右侧独立 switch”，既有楼层、恢复楼层和新建楼层均默认开启；切换当前楼层仍只切换唯一建造网格，开关只过滤对象，不删除或修改业务数据。三档文案更新为“仅当前层 / 多层透视 / 多层实显”；多层模式取消只看下层的限制，从 2F 可同时显示已开启的 1F 与 3F，从 1F 也能把已开启的 3F 纳入镜头。关闭 3F 后，透明与实显模式均拒绝显示 3F 对象；关闭任意一端时跨层传送带整体隐藏，避免关闭层几何泄露。纯函数回归验证当前层单层过滤、上下层对称显示、关闭层强制隐藏、跨层双端门禁和 `0–9m` 完整取景范围；TypeScript、AGV/无人机回归与生产构建通过。

机器中心、内部通道与端口专项复核：旧存档中的较小机器恢复后迁移为 6×6，机器中心保持整数网格点；建造栏同步显示“6×6 网格 · 3进3出”。Kenney 外壳与建造预览统一适配为 X/Z 5.4m、高 4.8m，四周保留约 0.3m 余量；三条机内带位于本地 Z=`-1/0/+1m`，长 4.32m、宽 0.88m。两端帘面位于 X=`±2.37m`，每侧 11 条、总宽约 3.45m、高 2.34m，完整遮挡三条通道。机内端口锚点距中心 2m；边界 `{x:10,z:10,width:6,depth:6}` 在 0° 时入料口 1–3 为 `(11,12)/(11,13)/(11,14)`，出货口 1–3 为 `(15,12)/(15,13)/(15,14)`；旋转到 90° 后分别为 `(12,15)/(13,15)/(14,15)` 与 `(12,11)/(13,11)/(14,11)`。工作灯、方向箭头、六个端口标签、选中标签与预览已同步调整高度和尺度。纯函数断言确认三条相距 1m 的并行路径互不碰撞，而共格、交叉、建筑覆盖传送带和建筑互叠均会被判定冲突；浏览器确认滚轮手持旋转和六端口状态面板。已占用端口在指针吸附与 Store 写入两层排除，删除传送带后自然释放。

货物仓库专项复核：候选 F 外壳、三条内部带、两端各 11 条黑帘、15 个 Kenney 纸箱容器和六个编号端口保持与机器相同的灰蓝/炭黑/黄色工业语言；场景标签只显示有限库存总数、无限项数和前两项物品，完整物品清单与操作收纳在选中仓库的右侧浮窗，没有把业务明细长期堆进中央画面。三仓道的入货/出货运动复用同一 TransitItem 路径，物品到达帘后锚点才入库；有限库存创建在途物品前扣减，无限供应作为明示仿真源不扣实际数量。Store 级测试确认仓库 A 的 5 件有限货物通过三条独占并行传送带全部进入仓库 B，最终 A=0、B=5、在途=0，总量保持 5；第四条重复端口连接被拒绝。库存状态不再使用含义混杂的单列“预留”：物流库存表分别展示“出库预留”和“入库占位”，仓库属性浮窗在物品行与容量摘要中同步显示两个方向；入库占位不改变已有货物的可出货量，出库预留则继续阻止传送带、手工调整和其他载具抢走已分配现货。

仓库库存管理延续同一浮窗语言：所有已定义物品以紧凑灰白行卡列出，名称、编码和数量建立清晰层级；“手动入库”与“无限供应”使用黄色表达可操作/启用状态，未定义物品时仅显示前置引导，不制造默认货物。场景标签显示前两项内容与无限项数，避免把完整明细长期压在 3D 中央画面。`infiniteSupply` 是显式业务状态而非超大伪数量；有限库存继续接受容量和守恒审计，无限供应出货不扣实际数量并以 `∞` 明示，关闭后恢复有限扣减。

本轮浏览器交互验收未调用截图：空仓属性浮窗先显示“还没有可入库的物品”和“前往定义物品”；创建 `CARGO-BOX / 包装纸箱货物` 后，同一仓库自动出现数量 0 的物品行。输入 12 并执行“手动入库”后，场景标签同步显示“包装纸箱货物 ×12”，容量显示 `12 / 1800`；启用无限供应后，场景和浮窗同时显示 `∞` 与“1 项无限”，实存 12 件保持不变且手动数量控件禁用；取消无限后仍恢复为 12 件有限库存。物流页的数量与供应方式列同步显示 `∞ / 实存 12 / 无限供应`，页面错误日志为空。Store 级回归同时确认三通道有限库存 A=0、B=5、在途=0、总量=5；无限供应源的实际数量始终为 0，30 秒内目标收到 30 件且仍有 6 件在途，无限配置中的 Item 删除被拒绝。该回归还修复了三出货口同轮读取旧库存导致有限库存可能短暂扣为负数的问题，现在每个端口派发前都会重新检查当前可用量。

仓库出货节奏与在途模型专项复核同样未调用截图：仓库属性浮窗默认显示每 `2.5` 秒出货，并可输入 `0.25–60` 秒或使用 `0.5/1/2.5/5` 秒快捷值；浏览器把测试仓库改为 `1` 秒后，约 `1.8` 秒观察到 2 件在途货物。Store 确定性回归让两个仓库分别以 `1` 秒和 `2.5` 秒出货，运行 3 秒后各自产生 3 件和 2 件在途货物，证明逐仓冷却互不阻塞。运行时映射已与核心 `catalog.json` 的 36 个稳定 `modelId` 和相对路径逐项一致，且所有目标 GLB 均存在；浏览器页面资源清单实际观察到 `package/pallet.glb` 与 `mechanical/spring.glb`，弹簧货物也已从源仓进入目标仓。正常定义物品不再使用白色方块；只有未知 `modelId` 或加载失败时才显示中性灰/黄色故障占位。页面错误日志为空。

参数化物品创建专项复核：创建弹窗延续灰白基底、细边框和黄色操作高亮，实时三维预览位于左侧，结构/材质参数以紧凑双列卡片组织在右侧，没有把 10 余项控件堆成无层级长表单；窄屏降为单列。运行时适配测试确认 36/36 个模型均可从同一浏览器生成入口构建；模型库全量验证覆盖 436 项 schema，其中 184/184 项几何参数均改变网格指纹。`BASIC_BOX` 的尺寸从 `[1,0.5,0.65]` 调整为 `[2.4,0.5,0.4]` 后包围盒同步变化，`PART_GEAR` 齿数从 8 调到 48 后三角形数从 224 变为 1008；圆柱体 `wallThickness` 仅在 `hollow=true` 时启用。保存结果只保留用户明确触碰的参数，未知键、质量等业务字段和旧版完整未调整默认表不会进入有效覆盖；显式设为默认数值的材质参数仍可生效。TypeScript 与生产构建通过。

配方与浮层命中专项复核：固定顶栏曾覆盖普通页面右上角主操作，导致点击“创建物品”或“新建配方”实际命中“运行”；现已为 `.page` 工作区补齐顶部安全区。配方编辑器和 Store 共同限制输入、输出各 `1–3` 种且不可重复，并校验物品引用、数量与处理时间。机器端口面板对单输出配方自动映射唯一产物；多输出配方只在已连接出货口显示产物选择，配方变化后清除失效分配，避免用不存在的业务产物驱动传送带。

弯道拼接专项复核确认 `conveyor-stripe-corner.glb` 原件为 1m × 1m。第一次修正仅根据边界顶点数量误把默认有效接口判断为 +X/-Z，实际渲染连接证明该判断正好反转 180°；ForgeCore 场景中未旋转接口现已纠正为 -X/+Z。随后复核发现右上/左下已经正确，但左上/右下的 Three.js Y 轴 `±90°` 符号互换；最终四向映射固定为 `-X/+Z → 0°`、`+X/+Z → +90°`、`+X/-Z → 180°`、`-X/-Z → -90°`。拐点前后半格裁切和相邻双弯短段省略规则保持不变。

货架专项复核（2026-08-15）：建造栏将“货物仓库（6×6 · 3进3出）”与“货架（8×2 · 无限堆叠）”作为两个并列且可辨识的建造项。浏览器从空白工厂放置货架后，属性浮窗确认独立 `8×2` 占地、`UNBOUNDED SHELF INVENTORY` 和 `0 / ∞` 容量；不再出现出货间隔、入货口或出货口，传送带候选也排除货架。mastjie `rack.glb` 的旋转后非等比缩放改到世界轴执行，消除了 X/Z 轴互换导致的异常长架体；可见长度约缩为修复前的一半，深度恢复为货架比例。四层、双排、五列共 40 个 Kenney 箱体以层板顶面标高为落点，横向覆盖架体并真实坐落在各层板上。箱体只承担“摆满货物”的视觉表达，场景标签和属性清单仍以 Inventory 为数量事实来源；货物仓库继续使用候选 F 外壳及 1800 件有限总容量。TypeScript 检查与生产构建通过，浏览器确认无货架端口文案且日志未出现应用级 error。

AGV 专项复核（2026-08-17）：建造栏明确显示“AGV（4×4 · A* 智能运输）”，Cels 主体按 `3.5×2.1×2.9m` 三轴等比放大目标呈现。选中车辆后的属性浮窗提供三节点可视化程序：起点、货物/每趟件数、终点，并可切换持续运输、起点库存或终点库存阈值。浏览器从空白工厂创建 2 个仓库、1 台 AGV 和 1 种验收货物，配置“起点库存达到 5 件、每趟 5 件”后投入 12 件；实际完成 2 趟、终点收到 10 件、起点剩余 2 件并进入等待触发，总量保持 12 件，页面错误日志为空。Store 场景另验证 2 台对向车辆最小中心距为 4m，双方各完成 10 趟且出现 9 个让行 tick，没有互相礼让死锁。

AGV 八方向寻路与动态绕行复核（2026-08-17）：建造栏和车辆程序状态改为“八方向 A*”，黄色路线允许按 `√2` 代价走斜线，同时禁止障碍角落切角。自动验证中，同一开放网格的路线由四方向 42m 缩短为 30.284m；任务开始后把未配置车辆移到既有直线路径中央，运输车横向绕开 5m、连续阻塞 0 tick、最小车心距 4m，并完整送达 20 件货物。双向会车场景最小车心距 3.955m、最大连续阻塞 1 tick，两车均完成运输且实际进入过固定通行权让行，没有互相礼让死锁。浏览器从空白工厂进入编辑器并放置、选中 AGV，确认建造栏显示“4×4 · 八方向 A*”，程序状态显示“八方向 A* 斜向最短路”，属性说明完整呈现停驻车辆动态绕行、红色无路和蓝色固定通行权语义，页面应用错误日志为空。

AGV 规划轨迹专项复核（2026-08-17）：当前可见楼层内所有已有真实路径的 AGV 都绘制深色托底、状态色虚线、方向箭头、节点与终点环；选中车辆额外显示取货点、卸货点或让行点标签及剩余距离。正常、阻塞和协调让行分别使用黄、红、蓝三种既有状态色。轨迹点序列固定为“车辆当前位置 → `waypointIndex` 指向的下一个未到达节点 → 后续节点”，不再把刚经过的上一节点接回首段；虚线相位与箭头统一由车辆向目标流动。新增纯函数断言以车辆位于 `x=1.5`、下一节点为 `x=2` 的中途状态确认输出为 `[1.5, 2, 3]`，不会错误生成 `[1.5, 1, 2, 3]`。TypeScript、AGV 导航回归与生产构建通过。

货架 AGV 与物品管理专项复核（2026-08-17）：AGV 起点和终点选择明确使用“货架 · 名称”与“仓库 · 名称”区分设施类型，选中货架后节点状态分别显示“货架取货”和“货架存货”，并说明装货真实扣减、卸货真实写入且货架仍不连接传送带。Store 确定性场景以货架 20 件为起点、仓库为终点完成 10 件运输后得到货架 10/仓库 10；反向以仓库为起点、货架为终点再运输 4 件后得到货架 14/仓库 6，车载为 0、总量保持 20。物品页面新增独立“已添加物品”管理区，浏览器实际创建 `SHELF-BIN` 方体并修改长度覆盖，随后将同一 Item 更名为“货架传动齿轮”、切换 `PART_GEAR`、设置 24 齿和 2.5kg，卡片同步显示新名称、模型、质量和参数覆盖数；未引用物品通过确认弹窗删除后计数恢复为 0。另建 `SHELF-CARGO` 并绑定 AGV 程序后，卡片显示“使用中：AGV 程序”且删除按钮禁用。编辑弹窗保留 Item ID 与既有引用，部分 schema 合法默认小数不再被 HTML step 误判为无效；页面错误日志为空。

无人机跨层运输专项复核（2026-08-17）：无人机属性浮窗复用 AGV 的三节点可视化任务结构，支持按楼层标识选择仓库/货架起点和终点、物品、每趟数量、持续运输或起点/终点库存阈值；默认飞行速度 4m/s、载重 30kg，实际件数继续按单件质量限载。场景以绝对楼层标高绘制无人机、真实 Item 载货模型和“当前机位 → 未到达节点 → 目标”的三维状态轨迹；黄色/红色/蓝色分别表达正常、无路和协调让行，虚线相位与三维箭头均从无人机向目标外流。路径使用 26 邻域，直线、面对角、体对角代价为 `1 / √2 / √3`，向下跨层只允许在已确认无建筑遮挡的列内垂直下降；传送带和 AGV 不写入空中障碍，机器、仓库、货架、缓存区与其他无人机会参与三维避障。自动回归的开放空间路线由轴向 63m 缩短为 41.148m并实际包含体对角；跨层载荷测试以 12kg 上限运输 5kg 物品，每趟正确限制为 2 件并完成库存扣减/写入；双机对向场景最小三维中心距 3.028m、双方各完成 1 趟并出现确定性让行，没有互相礼让死锁。AGV 建造和运行同时限制在 1F，上层放置请求会被拒绝。浏览器在已有多层工厂中实际放置 Count Infinity 无人机并选中属性面板，确认 `VISUAL DRONE PROGRAM`、任意楼层装卸提示、4m/s、30kg 与三维 A*/净空下降状态均显示；页面没有应用级 error。TypeScript、AGV 回归、无人机回归与生产构建全部通过。

无人机模型前向专项复核（2026-08-18）：Count Infinity 无人机建造预览和运行实体统一在视觉适配层绕 Y 轴旋转 180°，将原先前后颠倒的机头改为朝向 ForgeCore 载具航向；旋转值集中在同一常量，避免预览正确但运行实体仍反向。此次只调整模型视觉朝向，不改变业务航向、三维 A* 路径、避障、碰撞或库存运输结算。

AGV / 无人机路线半透明专项复核（2026-08-18）：两类载具共用路线透明度契约，选中路线的彩色虚线、外向箭头、节点与终点标记透明度为 `0.58`，未选中路线为 `0.34`；深色托底分别降至 `0.16 / 0.09`。目标标签背景同步降为半透明并减弱阴影。选中状态继续依靠线宽和较高透明度区分，但不再接近实色，从而避免路线覆盖建筑、货物和其他载具；黄/红/蓝状态、虚线外流方向和路径几何保持不变。

运行时性能专项复核（2026-08-18）：启动仿真后场景自动关闭实时投影并使用较低像素比，暂停后恢复用户选择的高质量阴影与清晰度；视图开关文案必须明确该运行时行为。未选中的机器、仓库和货架不常驻端口、库存等详细 HTML 标签，选中后完整恢复。传送带、设备内部流动标记及车辆路线箭头/节点改为实例化批绘；长路线只压缩严格共线节点并均匀降低未选中标记密度，路线的半透明状态、终点、转折和向外流动方向保持。逐层开关、三档多层显示和上下楼层可见规则不作 LOD 调整。

仓库逐端口出货节拍专项复核（2026-08-18）：仓库属性浮窗移除位于端口列表上方的统一出货间隔大面板，在“出货口 1 / 2 / 3”各自连接行内放置紧凑的“出货间隔”数值输入和“秒 / 次”单位。三项控件使用明确的端口编号 ARIA 标签；修改一个端口后另外两项保持原值。节拍输入与该端口连接目标、等待库存和输出物品状态放在同一视觉分组中，使用户能够直接理解设置作用范围。

无人机共享装卸位专项复核（2026-08-18）：货架与货物仓库上方的同一装卸坐标改为单机服务位；多机同时进场时，现有占位者先离场，否则按剩余航程和稳定 ID 确定唯一进场权，其他无人机在安全半径外显示蓝色协调让行状态及“共享装卸位”等待原因。卸货完成后增加“退出装卸位”阶段，路线终点标为“安全离场点”，无人机沿已验证的进场路径反向退出后才进入库存触发等待，不再停在终点永久占位。确定性回归以两架无人机从不同货架向同一货架分别存入 4 件货物，实际观察到排队，两架均完成 1 趟，终点两种库存各精确增加 4 件，最小三维中心距为 `4.243m`。

AGV 共享装卸位专项复核（2026-08-18）：货架与货物仓库按设施级单机服务位协调，即使八方向 A* 在设施不同侧找到多个停靠格，同一时刻也只允许一辆 AGV 进场装卸。装卸区内车辆和正在离场车辆优先，其余车辆按剩余路线与稳定 ID 排队，状态面板显示“共享装卸位”原因；卸货车辆沿原碰撞验证路线反向退出，轨迹目标显示“安全离场点”。恢复旧仿真时若发现已完成任务的 AGV 仍停在终点装卸区，会自动规划返回其安全停车位置。确定性回归以两辆 AGV 从不同货架向同一货架分别存入 8 件货物，实际观察到排队，双方均完成 1 趟，两种库存各精确增加 8 件，最小车心距为 `7.211m`。

初版审计不代表后续页面自动免检。每次新增页面、组件体系或重要视觉变化仍需按第 6 节清单逐项复核；浏览器截图只用于内部视觉 QA，不替代需求、可访问性或真实数据验证。

### 6.3 全站黑白工作区审计（2026-08-18）

| 检查项 | 结果 | 实现证据/说明 |
| --- | --- | --- |
| 全站视觉一致性 | 通过 | `src/styles.css` 建立黑、白、浅灰、炭灰令牌，账户入口、主菜单、顶栏、侧栏、总览、编辑器、物品、配方、仿真、物流和资产共用 Inter、细描边、弱投影和不超过 8px 的主要圆角；页面 UI 不再以黄色或渐变作为品牌主色 |
| 导航与壳层 | 通过 | `Topbar` 使用 ForgeCore 几何 Logo 和黑白胶囊；保存、主页、运行等清晰命令使用图标按钮并保留 `title`/ARIA 名称。`Sidebar` 删除工作区标题与版本说明，当前项改为黑色圆形反相，移动端改为不会被 Toast 遮挡的底部导航 |
| 主菜单与动效 | 通过 | `MainMenuPage` 使用与账户入口一致的全屏真实视频、白色渐变内容区和 `motion/react` 入场动画；继续存档、新建空白工厂、账户退出和工厂尺寸表单保持可用，没有加入营销解释卡片 |
| 页面信息密度 | 通过 | 七个业务页面删除不参与操作的英文眉题、能力宣言、治理说明、运行边界和重复解释段落；保留库存、配方、许可、路径、连接、模型、运行状态、表单字段和错误等真实业务信息。主要辅助字号不低于约 12px，空状态只保留下一步动作所需文案 |
| 编辑器场景优先 | 通过 | 全画布 3D、楼层栈、属性浮窗、建造/运行/视图工具坞和传送带栏继续叠放，不改变场景尺寸或业务交互；UI 浮层改为黑白灰，黄/青蓝/绿/橙/红仅保留给路线、运输、选择、协调和异常等场景语义 |
| 响应式与遮挡 | 通过 | 桌面端覆盖全部业务页面；移动端复核主菜单、总览和编辑器。390×844 视口下导航、正文和主要操作保持可见，Toast 上移至底部导航上方，固定顶栏和浮层不再遮挡可见主操作 |
| 业务与资产边界 | 通过 | 本轮只调整 React 组件呈现和 plain CSS；FastAPI 账户接口、远程/本地存档、确定性仿真、库存守恒、AGV/无人机寻路、36 个核心 Item 模型以及四套第三方资产许可状态均未改变 |

该审计取代 6.2 中关于“黄色承担品牌、导航和主操作高亮”的旧呈现约定；6.2 的其他业务交互、资产使用和场景语义记录继续有效。
